# EQMUX 화면·흐름 PRD — design.pen 기반

> **기준일** 2026-08-11 · **원본** [`docs/design.pen`](docs/design.pen)
> **역할** — 기능 PRD(`docs/prd/`)가 "무엇을 만들 것인가"를 FR 단위로 규정한다면, 이 문서는
> design.pen이 확정한 **사용자 맥락 → 화면 → 액션 → 상태 변화**의 계약을 기록한다.
> 화면·컴포넌트마다 design.pen 노드 ID를 병기해 추적 가능하게 한다.
>
> §5는 디자인이 기존 PRD의 **남은 결정에 답한 것**, §6은 **디자인과 PRD가 어긋나는 지점**(갱신 필요)이다.

---

## 1. 문서 범위

design.pen은 32개 최상위 노드로 구성된다.

| 분류 | 노드 |
|---|---|
| 순서도 | 사용자 맥락 순서도 (`oeCTX`) |
| 화면 15종 | 컨트롤 센터 · 워크스페이스 연결 · 팀 캐스팅 · 팀 편성 · 세션 상세 · 대화 · 트랜스크립트 · 역할 라이브러리 · 임무 배정 · git 패널 · 임무 패널 · 포트 패널 · 로그 패널 · 페인 배치 · Git Diff & 에디터 · 종료 확인 · 설정 |
| 재사용 컴포넌트 3종 | 앱 바 (`DdrkL`) · 터미널 워크스페이스 (`gMRz1`) · UI 프리미티브 (`ZwtYR`) |
| 보관 (설계 제외) | 대체된 관제 시안 (`gP5Dp`) · 종료 확인 V1 (`DTr6P`) · 제작 컨테이너 (`IXnXq`) |

섹션 프레임(00~07)은 캔버스를 사용자 여정 단위로 묶는 라벨이다 —
**01 첫 실행·팀 기동 / 02 일상 관제·세션 조사 / 03 팀·역할·임무 변경 / 04 워크스페이스 보조 도구 / 05 종료·설정 / 06 보관 / 07 재사용 컴포넌트**.

## 2. 사용자 맥락 흐름 — 순서도 (`oeCTX`)

순서도의 부제가 이 문서의 요약이다: **"버튼 → 화면 → 상태 변화 · 자동 실행 금지 경로를 포함한 전체 내비게이션 계약."**
7개 레인, 각 레인은 `진입 맥락 → [화면] → (버튼) → 결과` 구조다.

| # | 맥락 | 경로 | 계약 |
|---|---|---|---|
| 01 | **첫 실행** — 등록된 repo가 없음 | [워크스페이스 연결] →(저장소 연결)→ [팀 캐스팅] →(편성 선택)→ [팀 편성] →(팀 시작)→ 컨트롤 센터·터미널 | 4개 Claude Code 세션을 병렬 기동하고 관제 |
| 02 | **일상 관제** — 주의 세션을 빠르게 찾음 | [컨트롤 센터] →(세션 셀 클릭)→ [워크스페이스 터미널] →(세션 상세)→ [세션 상세] | waiting/dead 우선 확인 · 상세에서 상태·실행 플래그·메모리·이벤트 확인 |
| 03 | **역할·권한 변경** — 예: 린을 구현→리뷰로 | [역할 라이브러리] →(역할 저장)→ 재시작 필요 →(재시작)→ 재개 기반 재시작 | 파일은 즉시 갱신, 실행 플래그는 이전 값 유지 → 대화를 유지하고 새 권한으로 재기동 |
| 04 | **임무 운영** — 인증 리팩터를 노엘·린에 배정 | [임무 배정] →(임무 배정)→ 브리프 전달 →(브리프 전달)→ 터미널 | `roles/<세션>.md` 임무 블록 갱신 · 다음 턴부터 새 임무 수행 |
| 05 | **대화·근거 확인** — 결정 과정 되짚기 | [대화 탭] →(트랜스크립트 열기)→ [트랜스크립트 페인] →(메시지 전송)→ PTY 입력 | 타입 선택 후 @세션/@all 발신 · 뷰의 입력은 별도 경로 없이 PTY로 전달 |
| 06 | **종료·복구** — 필요한 세션만 복귀 | [종료 확인] →(종료)→ 완전 종료 →(앱 다시 실행)→ 복원·재개 제안 →(재개)→ 터미널 | flush→정상 신호→유예 후 강제 종료 · 레이아웃+500줄 복원 · **자동 재개 없음**, 선택한 세션만 resume |
| 07 | **오류·열화** — 경로·레지스트리·cwd 문제 | 상태 감지 오류 →(복구 옵션)→ [복구 선택] →(적용)→ 안전 상태 | 찾을 수 없음/degraded/cwd 불일치 → 경로 재지정·훅 폴백·빈 셸 유지 → 자동 실행 없이 계속 사용 |

**흐름 전반의 불변 원칙** (순서도가 범례로 명시): 화면 / 사용자 버튼 / 상태·분기 / 종료·오류를 구분하며,
상태를 바꾸는 것은 언제나 **사용자 버튼**이다. 자동 실행 경로는 존재하지 않는다 (C5 · D7 · FR-D-22).

## 3. 화면 명세

### 3.1 컨트롤 센터 (`bi8Au`) — 관제 기준 화면

순서도가 "관제 기준 화면"으로 지정한 허브. 좌→우 3열 구조: **팀·세션 카드 / 터미널·저장 상태·이벤트 / 인스펙터**.

| 영역 | 내용 |
|---|---|
| 헤더 | 타이틀 "관제 대시보드" + 액션 4종: `팀 추가` `워크스페이스` `PowerShell` `전체 종료` |
| 통계 타일 6종 | 워크스페이스 2 · 전체 세션 4 · 실행 3 · **재개 가능 2** · 저장 지연 42ms · 저장소 1.28G |
| 팀 카드 | 팀 뱃지(TEAM/02) · 이름 · `repo/Academy · main · 3/4 sessions` · 팀 전환(2/2 · NEXT) |
| 세션 카드 (팀당 ≤4) | 페르소나 이름 + 직책 뱃지(LEAD/IMPLEMENTER/REVIEWER) · 직무/페르소나 · 마지막 활동 · 스크롤백 줄 수(예: 18.4K lines) |
| 빈 슬롯 | `솔 캐스팅 — 검증 직무 + 페르소나 배정` 진입점 |
| 임무 패널 | 활성 임무 수 · 임무명 · `.eqmux/missions/*.md` 경로 · 상태(IN REVIEW) |
| 터미널 미리보기 | 스크롤백 seq 범위 표시(`seq 17491–17524 · 클릭하여 출력 이동`) · 파일↔캐시 동기화 확인 라인 |
| 저장 상태 | `WAL · 42ms` · `Academy/session.db · 324 MB · 64%` |
| SessionService 이벤트 | `GUI 1 · CLI 3` 접속 표시 · snapshot 재생/재개 가능/CLI send/배치 커밋/정리 회수 로그 |
| 인스펙터 | 선택 세션의 탭 2종(세션 정보/영속성) · 상태 `재개 가능` · 임무 배정 · 메타(팀·PTY·세션 ID·PID·셸·담당 폴더) · **복구된 직전 출력**(`──── APP RESTART · 500 LINES REPLAYED ────` 경계 표시) · 복구 액션 3종(**재개 가능 / 빈 셸 유지 / 강제 종료**) · CTA `이전 대화에 재개` · 영속성 타일(scrollback 12.9K lines · session.db 324 MB · WAL 42ms) |

관련 결정: FR-C-31·32(500줄 재생+경계), FR-C-33(재개 제안), FR-C-52(사용량 표시), FR-C-07(세션 통계), C7(GUI 1+CLI 다수).

### 3.2 관제 대시보드 — 다중 워크스페이스 뷰 (`sYRf5` 배경)

종료 확인 화면의 배경으로 그려진 12세션(3 워크스페이스) 대시보드. PRD G의 요구를 그대로 구현한 형태다.

- 요약 타일 4종: **총 세션 12 / BUSY 7 (서브에이전트 5) / WAITING 1 (사람의 응답 필요) / DEAD 1 (재개 가능)** — FR-G-06
- 정렬 토글 `주의 필요 순` — FR-G-07
- 워크스페이스 행 × 세션 셀 4: 페르소나·상태·임무만 (`busy · 서브 3`, `waiting · 12분째`, `dead · exit 1`, `빈 슬롯`) — FR-G-04·05·23·28
- 주의 & 이벤트 패널: `● WAITING · 12분째 — 노엘 · 승인 대기 · Bash(npm publish)` 상단 고정 + 시간순 이벤트 피드(상태 전이·서브에이전트·종료·임무 배정·역할 다시 읽음) — FR-G-40~42

### 3.3 워크스페이스 연결 (`mmBPm`)

- 부제가 도메인 모델 선언: **"git repo 1개 = 팀 1개 = 탭 1개 · 등록 무제한 · 동시 오픈 10개"** (W0 · B9)
- 액션: `+ 로컬 저장소 연결` · `원격에서 Clone`
- 등록 목록(24 등록 · 3 열림): 이름 · 경로 · 원격 · 브랜치+최근성 · 상태별 액션 — `열림`(OPEN) / `열기` / **`재지정`(경로를 찾을 수 없음)** ← 흐름 07·FR-E-08 대응
- 선택 상세: 브랜치 · 원격 · **팀 편성 `.eqmux/team.json`** · 세션 4/4 · 마지막 사용
- 원칙 카드: **"파일이 원본입니다"** — team 파일 로드 + 역할 파일 실측

### 3.4 팀 캐스팅 (`IvduM`)

- 프리셋 4종: **표준 / 집중구현 / 리뷰중심 / 탐색**
- 4슬롯 카드: 슬롯 번호 · 직책 뱃지(LEAD/BUILDER/REVIEW) · 페르소나+직무 · 페르소나 힌트 1줄(P3 예산) · **실행 권한 미리보기**(`write ✓ · commit ✓ · push —`) · `캐스팅 변경`
- 푸터: **`.eqmux/team.json`과 `team.md`에 저장 · roles/는 합성 후 gitignore · 사용자의 CLAUDE.md는 수정하지 않음** (FR-E-70)
- CTA: `편성 저장` · `4개 세션 시작` (FR-D-07 병렬 기동으로 이어짐)

### 3.5 팀 편성 (`nspu3`)

- 부제: **"`.eqmux/team.json` 원본 · team.md 파생 · LEAD 최대 1명"**
- 4슬롯 + 관계 표기: 범례 **보고 → · 지도 ⇢ · 리뷰 ◇ · 협업 —** (+검증), 멤버별 관계 2줄
- 파일 계약 문구: "파일이 원본 · **중복 관계와 자기 참조는 저장 시 제거** · team.json/team.md는 커밋"
- 선택 상세(예: 린·리뷰): `write/commit/push = false` + **`permission-mode: manual`** 번역 결과 노출 (FR-D-40·41)
- 경고 카드: **"권한 변경은 재시작 필요 — 파일은 즉시 저장되지만 실행 플래그는 프로세스 수명 동안 고정"** (E11′ · FR-D-42)

### 3.6 세션 상세 (`lefeD`)

- 헤더: `노엘 · 구현` · **`Claude Code 2.1.226 · session 8d19…`** · 상태 뱃지 `WAITING · 12분째`
- 탭 3종: 정보 / 이벤트 / 대화
- 대기 카드: `승인 대기 — Bash(npm publish)` + **"승인·거부는 터미널 페인에서 수행합니다"** (G7 — 대시보드 승인 없음)
- 정보 필드: 상태 `waiting` · 임무 · 역할 · 서브에이전트 0 · **메모리 `428 MB · peak 611 MB`** (C11 · FR-G-66) · 재개 `가능 · transcript + cwd 일치` (FR-G-64)
- **실제 실행 플래그** 원문 노출: `--permission-mode acceptEdits --disallowedTools Bash(git commit *) Bash(git push *) --sess…` (FR-D-41)
- `권한 변경 감지 · 재시작 필요` 배지 + `대화 유지 재시작` (FR-G-63)
- 액션: **페인으로 점프 / 재개 / 중지** — G7의 3종 그대로

### 3.7 대화 탭 (`KnvN6`)

- 헤더: 미확인 수 · 필터(전체 / 임무 / 미확인) — M5(스레드 없음, 임무 필터)
- 메시지: `발신 → 수신` · **타입 뱃지(HANDOFF/ESCALATE/REPORT/REVIEW)** · 시각 · 본문 (M2)
- 발신: **타입 5종 선택이 선행**(ASK/HANDOFF/REPORT/REVIEW/ESCALATE) + `@카이 에게 물어볼 내용을 입력…` (M2 · M4)

### 3.8 트랜스크립트 페인 (`T1Ux5`)

- 소스 표시: `~/.claude/projects/C--workspace-Academy/8d19.jsonl · **참조만**` (V2) + 상태 `1급 소스 · JSONL · offset window · 스크롤백 폴백 준비됨` (V1 · FR-G-86)
- 턴 단위: USER / 어시스턴트(모델·소요 표시) / **도구 호출 접힘**(`▸ Read · src/auth/session.ts · 184 lines`) (FR-G-82·83)
- 파싱 실패 알림: `1개 JSONL 라인을 건너뜀 · 파싱은 계속됩니다` (FR-G-85)
- 우측 **턴 개요 레일**: 시각 + 요약(요청/분석/결정/handoff) — 긴 대화 탐색용
- 하단 입력: `PTY로 입력 전송…` (V3)

### 3.9 역할 라이브러리 (`GX8Ly`)

- 부제: "전역 + 워크스페이스 오버라이드 · 직무와 페르소나 분리" (P1 · P2)
- 직무 4 SEED: 리드(write✓·commit✓) / 구현(write✓·commit—) / 검증·리뷰(write—·manual)
- 페르소나 8 GLOBAL: 이름 + 힌트 1줄 (카이·노엘·린·솔·미라…)
- 에디터: 스코프 표시(`WORKSPACE OVERRIDE · jobs/lead.md`) · ID · 기본 권한 · 책임 · **금지**(예: "검증 없이 완료 선언 · 원격 push")
- 경고: "permissions 변경은 재시작 필요 — 재개 기반 재시작 후 적용" (E11′)

### 3.10 임무 배정 (`xdfwU`)

- 부제: **"`.eqmux/missions/*.md`가 원본 · 역할 파일 임무 블록에 멱등 반영"**
- 임무 목록: 상태 4종 **TODO / IN PROGRESS / IN REVIEW / DONE**
- 브리프 에디터: 목표 / 산출물 / **브랜치 `(선택 연결)`** (E12 잠정안 채택) · 파일 감시 상태(`파일 변경 감시 중 · 마지막 실측 10:42:18`)
- 세션 배정: 세션별 토글(`● 배정됨 / ○ 미배정`) · 리드는 `인지 전용` 표기 가능
- CTA: `파일에 저장` · **`배정 저장 & 브리프 전달`** — 힌트 "배정 시 역할 파일 갱신 + 자연어 브리프 전달"

### 3.11 사이드 패널 4종 — 보조 도구 (Section 04)

**임무 패널 (`ehpqx`)** — 임무 + 파일 탐색기 결합. 현재 터미널 연동(`CURRENT TERMINAL LINKED` · 세션 cwd·브랜치),
`.eqmux/` 트리(team.json·team.md·roles·missions), 임무 md 미리보기(frontmatter: `status / branch / assigned / updated`),
**`WATCHING · .eqmux/` + "외부 편집 감지 · 파일 우선"** (FR-C-23), 액션 `파일 열기` `브리프 전달`.

**git 패널 (`PAlmZ`)** — 브랜치 + ahead/behind(↑4 ↓0) · 액션 4종(diff/pull/commit N/push N) ·
변경 요약(변경/추가/수정/삭제) · 커밋 그래프(태그 main·origin/main) · diff 뷰 진입(`25개 변경 파일을 3분할 diff로 검토`).

**포트 패널 (`rBkF0`)** — **세션 포트 / 시스템 포트 2단** 분리, 포트별 프로세스·소유 세션·바인딩 주소, `5s POLL`,
요약(세션 3 · 시스템 28 · 충돌 0 · **외부 노출 0**) + 보안 문구 "모든 개발 포트가 localhost에만 바인딩".

**로그 패널 (`KcL3j`)** — 레벨 필터 + 검색, INFO/WARN/ERROR 카운트, 분류 태그(앱/세션/store/agent/git),
실시간 스트림 표시(`4.2 events/s`). PRD C `event` 테이블·FR-G-40의 표면.

### 3.12 페인 배치 (`srpYm`)

- 레이아웃 6종: **그리드 열 우선 / 그리드 행 우선 / 세로 스택 / 가로 나열 / 메인+우측 스택 / 메인+하단 나열**
- 현재 세션 수 기준 미리보기(`3 PANES`) · 선택 즉시 미리보기 · **적용 후 분할선 드래그로 비율 조정**
- 단축키 `CTRL+SHIFT+L`

### 3.13 Git Diff & 에디터 (`AXXhV`)

- 좌: 변경 파일 목록(M/A/D + ±줄수) · `모두 스테이지` `커밋`
- 우: **나란히 비교** — BASE(커밋 · 읽기 전용) ↔ **WORKTREE(편집 가능 · 커서 위치 표시)**
- 액션: `변경 스테이지` `현재 저장` `선택 변경 되돌리기`
- 의미: 보조 페인은 단순 뷰어가 아니라 **워크트리 쪽은 편집 가능한 에디터**다 (H2·H5에 대한 답)

### 3.14 종료 확인 (`sYRf5`)

- 제목/부제: "EQMUX를 종료할까요? — 실행 중인 에이전트 4개가 종료됩니다. **자동 재개하지 않습니다**"
- 종료 시퀀스 고지: **입력 차단 → 저장소 flush → 정상 종료 신호 → 유예 후 강제 종료** (FR-C-62 문구 그대로)
- 세션 목록: 이름 · 워크스페이스 · 상태 · **재개 가능 여부**(`재개 불가 · transcript 없음` 사유 포함) (FR-C-61 · FR-D-20)
- 액션: 취소 / 종료

### 3.15 설정 (`o2bCs2`)

탭 5종: 일반 / 에이전트 런타임 / 알림 / 저장소 / 보안. 표시된 기본값이 곧 정책 선언이다.

| 섹션 | 항목 → 기본값 | 근거 |
|---|---|---|
| 시작과 복원 | 시작 화면 `관제 대시보드` · 마지막 탭 복원 `꺼짐` · 스크롤백 재생 `500 lines` | FR-G-02 · FR-C-31 |
| 알림 | OS 알림 `waiting · dead` · 창 포커스 시 `억제` · waiting 사운드 `꺼짐` | G3 · G6 |
| 저장소 | 세션당 `100,000 lines` · 워크스페이스 `500 MB` · 전역 `2 GB · 30 days` | FR-C-50 |
| 런타임 | CLI 버전 `2.1.226 verified` · 상태 소스 `registry + hook` · watch 폴백 `2s polling` | FR-D-62 · FR-D-10·11 |
| 권한 정책 | 기본 구현 `acceptEdits` · 리뷰·검증 `manual` · 권한 변경 `재개 기반 재시작` · **"bypassPermissions는 앱에서 제공하지 않습니다"** | §4.5.1 · E11′ |
| 보안 | `~/.claude/settings` `수정 안 함` · repo `.claude` `수정 안 함` · Hook settings `세션별 추가` | D3 · FR-D-30~32 |

## 4. 재사용 컴포넌트

**앱 바 (`DdrkL`)** — 좌측 탭: **관제(고정) + 워크스페이스 탭**(EQMux · Academy…) (G1 · B3).
우측 도구 토글 9종: **배치 · 임무 · git · 포트 · 로그 · 브라우저 · 팀 · 대화 · 설정**.

**터미널 워크스페이스 (`gMRz1`)** — 팀 컨텍스트(팀명 · N SESSIONS · 배치 모드) + 세션 페인(이름 · RUNNING ·
셸 버전 · 프롬프트) + 상태 바(`◉ PTY 4 ONLINE · 배치 · OUTPUT KB · 현재 경로`).

**UI 프리미티브 (`ZwtYR`)** — SYSTEM 1.0. 컬러 토큰 7종(BG · SURFACE · BLUE · PURPLE · GREEN · AMBER · RED),
타이포·컨트롤 샘플. 주의 예산 원칙과 결합해 **경고 계열(AMBER=waiting, RED=dead/오류)만 유채색**으로 쓴다.

## 5. 디자인이 확정한 결정 — 기존 PRD의 "남은 결정"에 대한 답

| 기존 ID | 항목 | 디자인의 답 | 근거 화면 |
|---|---|---|---|
| B1 · B5~B8 | 레이아웃 모델 | **2×2 고정이 아니라 6종 배치 + 분할선 드래그.** 복원 정책은 "화면 상태만 복원 · 마지막 탭 복원 기본 꺼짐" | `srpYm` · 설정 |
| H1 | 패널 선정 | **배치 · 임무(탐색기 겸용) · git · 포트 · 로그 · 브라우저 · 팀 · 대화 · 설정** 9종 | 앱 바 |
| H2 · H5 | 에디터/diff 깊이 | diff는 나란히 비교 + **워크트리 쪽 인라인 편집·스테이지·되돌리기**까지 | `AXXhV` |
| W3 | team 파일 커밋 여부 | **team.json · team.md 커밋, roles/는 합성 후 gitignore** | `IvduM` · `nspu3` |
| E12 | 임무↔브랜치 강제 | **선택 연결** (`branch optional`) — 잠정안 확정 | `xdfwU` |
| G 미해결 3 | 비용/계측 노출 | 세션 상세에 **메모리 현재·피크 표시** (C11 · FR-G-66 구현 확인) | `lefeD` |
| 미해결(임무 상태) | 임무 상태 어휘 | **TODO / IN PROGRESS / IN REVIEW / DONE** 4종 + frontmatter(`status/branch/assigned/updated`) | `xdfwU` · `ehpqx` |
| — | 캐스팅 프리셋 | **표준 / 집중구현 / 리뷰중심 / 탐색** 4종 | `IvduM` |
| — | 관계 어휘 | **보고 / 지도 / 리뷰 / 협업 / 검증** + LEAD 최대 1명, 중복·자기 참조는 저장 시 제거 | `nspu3` |

## 6. 디자인 ↔ 기능 PRD 불일치 — 갱신 필요

| # | 불일치 | 내용 | 처리 방향 |
|---|---|---|---|
| Δ1 | **팀 파일 원본** | PRD E11은 `team.md`를 원본으로 규정. 디자인은 일관되게 **`team.json` 원본 · `team.md` 파생** | E11 개정 필요 — 구조 데이터(json)와 에이전트가 읽는 파생(md)의 분리가 디자인의 의도 |
| Δ2 | **D2 위반 목업 카피** | 로그 패널에 `AgentCommender 시작`, `Vision (Codex CLI)`, `Game (Gemini CLI)` 세션 생성 로그 — D2(Claude Code 단일)와 충돌. 컨트롤 센터 복구 액션의 노드명에도 Codex/Gemini 잔재 | design.pen 목업 카피 교체 (기능 변경 아님, 레퍼런스 제품 카피 잔재) |
| Δ3 | **통계 타일 어휘** | 컨트롤 센터(`bi8Au`)는 6타일(재개 가능·저장 지연·저장소 포함), FR-G-06은 4타일(총·busy·waiting·dead). 다중 워크스페이스 뷰(§3.2)는 FR-G-06과 일치 | 컨트롤 센터=단일 팀 운영 뷰, §3.2=다중 워크스페이스 관제 뷰로 **역할 분리를 PRD G에 명문화** 필요 |
| Δ4 | **`전체 종료` 버튼** | FR-G-55는 일괄 액션 배제. 컨트롤 센터 헤더의 `전체 종료`가 세션 일괄 중지라면 충돌, 앱 종료(C10)라면 무해 | 의미 확정 필요 — 앱 종료(종료 확인 다이얼로그 진입)로 정의 권장 |
| Δ5 | **원격 Clone** | `원격에서 Clone`은 기존 PRD 어디에도 없는 신규 기능 | PRD E(워크스페이스 연결) FR 추가 필요 |
| Δ6 | **상태 어휘 혼용** | 터미널 페인 상태가 `RUNNING`으로 표기 — §4.2.1의 6종(busy 등)과 다른 어휘 | 목업 카피를 상태 모델 어휘로 통일 |
| Δ7 | **브라우저 패널 화면 부재** | 앱 바에 `브라우저` 토글이 있으나 해당 화면 시안이 없음 | 후속 시안 또는 1차 범위 제외 명시 |

## 7. 수용 기준 (화면·흐름 관점)

1. 순서도(§2)의 7개 레인 모두에서, 상태를 바꾸는 전이는 사용자 버튼으로만 발생한다. 자동 실행 경로가 없다.
2. 첫 실행 흐름(레인 01)은 화면 3개·버튼 3번으로 컨트롤 센터에 도달한다.
3. `waiting` 세션은 다중 워크스페이스 뷰에서 유일하게 유채색이고, 대기 대상과 경과 시간이 셀에서 읽힌다.
4. 세션 상세에서 실행 플래그 원문 · 메모리(현재·피크) · 재개 가능 근거(transcript+cwd)가 항상 보인다.
5. 권한 변경 시 두 화면(팀 편성 · 역할 라이브러리) 모두 "재시작 필요" 경고가 뜨고, 재시작은 대화를 유지한다.
6. 종료 다이얼로그는 세션별 재개 가능 여부와 불가 사유를 표시하며, 종료 시퀀스 4단계를 고지한다.
7. §6의 Δ1~Δ7이 해소되기 전에는 해당 화면을 구현 스펙으로 사용하지 않는다.

---

> **다음 단계 제안** — Δ1(team.json 원본)은 PRD E·인덱스의 E11 개정으로, Δ3·Δ4는 PRD G 개정으로,
> Δ2·Δ6은 design.pen 카피 수정으로 처리한다. §5의 확정 사항은 00-index.md §5(남은 결정)에서 지울 수 있다.
