# 2026-08-10 세션 기록 #1

> **프로젝트** EQMUX — 멀티 에이전트 MUX (그린필드, 커밋 1개 상태에서 시작)
> **범위** 레퍼런스 조사 → 기능 비교 → 기술 스택 도출 → PRD 대분류 → PRD C 작성 → 워크스페이스 모델 재정의
> **상태** PRD C 초안 완료 · **워크스페이스 모델 변경과 역할 설계는 문서 미반영** (§4 참조)

---

## 0. 한 줄 요약

레퍼런스 5종을 조사·비교해 기술 스택을 확정하고, 제품을 11개 PRD로 쪼갠 뒤 첫 PRD(C 세션·영속성)를
작성했다. 이후 "워크스페이스 = git repo = 팀 = 세션 4개" 로 도메인 모델을 재정의했고,
그에 따른 문서 갱신은 **아직 하지 않았다**.

---

## 1. 산출물

| 파일 | 내용 | 상태 |
|---|---|---|
| `docs/terax-features.md` | Terax 기능 명세 (terax.app/docs 22페이지 조사) | 완료 |
| `docs/features-tree.md` | 5개 문서 전체 기능을 트리 한 장으로 통합 + 축별 교차표 | 완료 |
| `docs/features-comparison.html` | 기능 매트릭스(29축) + 권장 기술 스택 + 아키텍처 + 성능 예산 + 안티패턴 | 완료 |
| `docs/prd/00-index.md` | PRD 대분류 맵(11개) + 결정 목록(78개) + 작성 순서 + 템플릿 | **갱신 필요** |
| `docs/prd/12-session-store.md` | PRD C — 세션 & 영속성 (FR 45개, 수용 기준 10개) | **갱신 필요** |

기존 문서(입력으로만 사용): `ghostty-features.md` · `orca-features.md` ·
`bbarit-agent-oss-features.md` · `agentManager-features.md`

---

## 2. 대화 흐름

### T1. Terax 기능 명세서 작성

`docs/` 3개 문서(ghostty · orca · bbarit)의 양식을 따라 terax.app/docs를 조사해 명세 작성.
22개 페이지 전부 확인(Overview 3 · Features 7 · AI 6 · Reference 5).

분류는 기존 양식대로 `🖥️ 터미널 / 🤖 에이전트 / 📦 기타`.
문서에 없는 항목(슬래시 커맨드 목록, 음성 트랜스크립션 모델)은 "미공개"로 명시.

### T2. 5개 문서 통합 트리

`agentManager-features.md`가 새로 추가된 것을 확인하고 5종을 대상으로 작성.
각 제품의 자체 분류를 유지한 채 리프까지 전개 + 기능 축 × 제품 교차표(21축).

교차표에서 드러난 것 — 겹치는 축은 터미널·git·브라우저 정도이고 나머지는 거의 배타적.
워크트리·모바일은 Orca 단독, 팀·직책·관계도는 AgentCommender 단독,
페르소나 295종은 bbarit 단독, OSC 133/777 상태 감지는 Terax 단독.

### T3. HTML 기능 비교 + 기술 스택 권장

자체 완결형 HTML(외부 리소스 0, 검색·필터, 라이트/다크 토글, 인쇄 대응) 작성.
세 기준 중 "성능 최소화"는 **번들·설치 용량 및 상주 리소스 최소화**로 해석하고 진행.

**권장 스택** — Tauri 2(Rust) / portable-pty / vte·termwiz(헤드리스 VT) /
xterm.js+WebGL(가시 페인만) / SolidJS / CodeMirror 6 / SQLite+JSON /
git CLI+gix / Tauri Channel 바이너리 IPC / named pipe+NDJSON

**멀티 에이전트라서 달라지는 3가지**
1. 헤드리스 VT — 비가시 페인의 렌더러 비용을 0으로
2. 데몬이 PTY 소유 — 앱과 무관하게 에이전트 생존
3. 프레임 코얼레싱 + 드롭 허용 — 폭주 출력에서 UI 정지 방지

**제약 2가지** — WebGL 컨텍스트 상한(대략 16, 실측 필요) · Tauri 자식 웹뷰 unstable 플래그

### T4. PRD 대분류 + 판단 항목

11개 PRD를 4영역(기반·에이전트·도구·운영)으로 분류하고, 결정 항목 78개에 `A1`~`K7` ID 부여.
축 결정 5개를 식별해 4개를 질의(K1은 Windows 우선으로 가정 확정).

→ **C1 · D1 · E1 · B3 확정** (§3)

C1은 권장(별도 데몬)과 다른 선택이라 파급을 정리하고 보완책 3개 신설
(D10 세션 재개 P0 승격 · C9 모듈 경계 · C10 종료 동작).

### T5. SQLite 데이터 설명 + PRD C 작성

**저장소 3분할 원칙** — 누가 읽는가로 매체가 갈린다.
파일(에이전트+사람) / JSON(사용자 편집) / SQLite(앱 전용·대용량·조회)

**SQLite 7종** — 스크롤백 · 세션 메타 · 이벤트 로그 · 명령 히스토리 ·
에이전트 세션 매핑 · 임무 배정 캐시 · 알림

**결정적 설계 포인트** — PTY raw 바이트를 그대로 저장하면 안 된다.
에이전트 CLI는 TUI라 리페인트가 폭증하므로, **VT를 통과해 확정된 줄만** 적재한다.
이러면 용량이 예측 가능해진다(줄당 ~150B × 세션당 50,000줄 ≈ 7.5MB).

→ **C4 · C5 · C7 · C10 확정**, C3 · C6 · C9는 권장안 채택 → **C 영역 결정 종료**
→ `12-session-store.md` 작성 (FR 45개 / P0 27 · P1 15 · P2 3)

C10=완전 종료로 "앱 종료 = 모든 에이전트 종료"가 확정되어,
D10(세션 재개)이 유일한 연속성 장치가 됨. 재개 불가 CLI는 D2 선정 기준에 반영 필요(리스크 R2).

### T6. 세션 4개 한정 시 영향 분석

**가장 큰 변화** — "비가시"의 정의가 페인 → **비활성 탭**으로 이동.
4세션이면 전부 동시에 보이므로 페인 단위 절약은 사라지지만, 탭 단위로 옮겨가며 논거는 더 강해진다.

**가독성 근거** — 1920×1080에서 8세션(2열×4행)은 페인당 114열×14행,
4세션(2×2)은 114열×28행. 에이전트 TUI는 14행에서 사실상 못 읽는다.

자동으로 쉬워지는 결정 — B1(2×2 고정) · A3(WebGL 상한 해소) · A2(스크롤백 상향 여지) ·
G1(여러 워크스페이스 동시 나열) · E2 · E5 · D7

새로 필요한 결정 3개 제시 — ① 탭 상한 ② PTY 기준 vs 페인 기준 ③ 하드/소프트

### T7. 워크스페이스 모델 재정의

사용자 답변:
1. **워크스페이스 10개 상한** + 연결된 워크스페이스 리스트 → 선택 → 팀 연동 시스템 필요
2. **워크스페이스 = git에 연결된 폴더 1개**를 관리하는 방식으로 변경
3. **하드 상한**

**계층 변경**

```
이전                              지금
워크스페이스 루트 (폴더)           워크스페이스 = git repo (디스크 어디든) = 팀 = 탭
└─ 팀                             ├─ 세션 1~4        ← 하드 상한
   ├─ 임무 A (폴더)               └─ 임무 = repo 안의 작업 단위(브랜치)
   ├─ 임무 B (폴더)
   └─ 세션 1~8 (담당 폴더)         앱: 워크스페이스 최대 10개 동시 오픈
```

**얻는 것** — 모든 패널 스코프가 통일된다. git 저장소 선택 UI가 통째로 사라지고
(AgentCommender의 `listGitRepos` 탐색 로직 소멸), 파일 탐색기·diff·프리뷰·검색이 전부 repo 기준.

**깨지는 것 2개**
1. **세션별 담당 폴더·CLAUDE.md가 성립 안 함** — 4세션이 같은 cwd면 같은 파일을 읽는다.
   동시에 4명이 같은 파일을 고치는 충돌 문제도 함께 발생
2. **임무 정의 변경** — "팀 아래 프로젝트 폴더" → "repo 안의 작업 단위(브랜치+목표)".
   기존 "새 임무 만들기 3갈래"는 **워크스페이스 등록** 기능으로 승격

**예산 재계산** — 최대 10×4 = 40세션. 활성 4가시 + 비활성 36 ≈ 364MB (예산 내).
단 실질 제약은 앱이 아니라 **에이전트 CLI 프로세스 자체**(각 수백 MB + API 한도).
→ 열림 상한과 실행 상한을 분리해야 함 (신규 D11)

### T8. 세션별 역할 부여 설계 (제안, 미확정)

**핵심 원칙 — 파일이 원본, 프롬프트는 포인터.**
역할을 프롬프트에 통째로 넣으면 컨텍스트 압축 때 사라지고 매 턴 토큰을 먹는다.
파일에 두고 "읽어라"만 주입하면 필요할 때 다시 읽고, 역할 변경 시 재시작이 불필요하다.

**3층 구조**

```
<repo>/
├─ CLAUDE.md            층1. 프로젝트 규칙 · 커밋 · 전원 공유
└─ .eqmux/
   ├─ team.md           층2. 편성표(직책·보고 관계) · 커밋
   └─ roles/<세션>.md   층3. 세션 전용 역할 · gitignore
```

`.eqmux/roles/<세션>.md`로 통일하면 **워크트리 모드와 repo 공유 모드 양쪽에서 동일하게 동작**한다.
격리 방식(E1')이 바뀌어도 역할 체계는 흔들리지 않는다.

**역할 파일 구성** — 직책 / 책임 / 권한(쓰기·커밋·푸시) / 관계 / 금지 / 임무 블록
(임무는 별도 파일이 아니라 역할 파일 안의 블록 — 읽어야 할 파일 수를 늘리지 않기 위해)

**주입 3중 폴백**
1. 시스템 프롬프트 플래그 (CLI별 지원 편차 — D2에서 실측 확정)
2. 부트스트랩 프롬프트 1회 (모든 CLI 동작, 실질 기본값, 2줄로 짧게)
3. 훅으로 매 턴 리마인더 (지원 에이전트만)

**세션 시작 시퀀스** — 워크트리 생성 → 역할 파일 기록 → PTY 생성 → 환경변수 주입
(`EQMUX_SESSION` · `EQMUX_ROLE_FILE` · `EQMUX_TEAM_FILE` · `EQMUX_TERMINAL`)
→ 에이전트 실행 → 부트스트랩 → **이후 변경은 파일만 갱신 + "다시 읽어라" 한 줄**

**읽기 전용 역할의 강제 2단** — 약한 규범(역할 파일의 금지 항목) +
강한 강제(그 세션만 자동승인 없이 실행). 즉 **역할이 실행 권한 모드를 결정**한다 → D5의 답이기도 함

**4슬롯 프리셋** — 표준(리드1·구현2·검증1) / 집중구현(리드1·구현3) /
리뷰중심(구현2·리뷰2) / 탐색(리서치1·구현1·검증1·문서1)

**약점 2개** — 에이전트가 파일을 안 읽을 수 있음(완화: 짧게 유지 + 훅) ·
권한 규칙은 규범이지 샌드박스가 아님(워크트리 격리가 유일한 구조적 완화)

---

## 3. 확정 결정 누적

### 축 결정

| ID | 결정 | 확정안 | 비고 |
|---|---|---|---|
| C1 | 데몬 분리 | **앱 내장** (Tauri Rust 백엔드가 PTY 소유) | 권장(별도 데몬)과 다른 선택 |
| D1 | 내장 AI 에이전트 | **1차 외부 CLI 전용**, 이후 확장 | J2 연동 — 1차에 API 키 미보유 |
| E1 | 코드 격리 | **하이브리드** (세션별 폴더 + 워크트리 옵션) | T7로 재해석 필요 → E1' |
| B3 | 탭의 의미 | **팀=탭 + 페인 타입 다양화** | B4 자동 확정(허용) |
| K1 | 타깃 플랫폼 | **Windows 우선** | 가정 확정 |

### C 영역 (전부 종료)

| ID | 확정안 |
|---|---|
| C3 | 하이브리드 — 세션·스크롤백·이벤트=SQLite / 설정·레이아웃=JSON / 팀·임무=파일 |
| C4 | 상한 자동 정리 — 세션당 50,000줄 · 전역 2GB · 30일 |
| C5 | 레이아웃 + 스크롤백 재생 + 재개 제안 (에이전트 자동 재실행 안 함) |
| C6 | 스키마만 원격 대응 |
| C7 | GUI 1 + CLI 다수 (같은 OS 사용자 제한) |
| C9 | `SessionService` 모듈 경계 채택 |
| C10 | 항상 완전 종료 + 확인 다이얼로그 (트레이 상주 없음) |
| ~~C2·C8~~ | 폐기 (C1=② 로 무의미) |

### T6~T7 신규 확정 (문서 미반영)

| ID | 결정 | 확정안 |
|---|---|---|
| B2 | 워크스페이스당 세션 상한 | **4 · 하드 상한** |
| **B9** | 워크스페이스 동시 오픈 상한 | **10 · 하드 상한** (이 기록에서 ID 신규 부여) |
| **W0** | 워크스페이스 정의 | **git repo 1개 = 팀 1개 = 탭 1개** (신규 부여) |
| — | 보조 페인 카운트 | 에디터·diff·브라우저는 **세션 슬롯을 소비하지 않음** |

---

## 4. 미반영 변경 — ✅ 반영 완료 (세션 말미)

> T6~T8의 결정을 전부 문서에 반영했다. 추가로 T9에서 **관제가 핵심, 대화는 사이드 탭 부가 기능**으로
> 위치가 정정됐고(내가 메신저를 제품 중심으로 과대 해석했던 것), 그 정정도 반영됐다.
>
> - `docs/prd/00-index.md` — **전면 재작성**. 제품 정의 · 도메인 모델 · 확정/미확정 결정 · **화면 인벤토리** 추가
> - `docs/prd/12-session-store.md` — 워크스페이스 모델 반영 (FR-C-10 · FR-C-20a · FR-C-50 · §5 · §7 · 수용 기준)
> - `docs/prd/21-team-mission.md` — **신규 작성** (PRD E · FR 55개)
> - `docs/features-comparison.html` — §3.1 · §6 예산표를 4세션 × 10워크스페이스 기준으로 갱신
>
> 아래는 당시 식별했던 작업 목록(기록 보존):

### `docs/prd/00-index.md`
- B2=4 · B9=10 · W0 확정 기록
- E 영역 항목 재작성 (임무 정의가 폴더 → 브랜치로 바뀜)
- 신규 항목 추가 — E1'(세션 격리 방식) · E11(역할 파일) · E12(임무-브랜치 연결) ·
  W1(레지스트리) · W2(git 패널 대상) · W3(team.json 위치) · D11(실행 중 에이전트 총량)
- B1 · A2 · A3 · G1 · E2 · E5 · D7 — 4세션 확정으로 답이 좁혀진 항목 갱신

### `docs/prd/12-session-store.md`
- FR-C-10 인메모리 2,000줄 → 상향 검토
- FR-C-50 전역 2GB 유지하되 세션당 상한 상향
- §5 비기능 "8세션 동시 폭주" → "활성 4세션 + 비활성 탭 N" 기준으로 재정의
- 수용 기준 1 — `yes` 8개 → `yes` 4개 × 워크스페이스 3개
- §7.2 B와의 계약 — "페인 가시성" → "탭 활성 변화"

### `docs/features-comparison.html`
- §6 예산표를 4세션 × 10워크스페이스 기준으로 갱신

---

## 5. 미해결 / 확정 대기

| ID | 항목 | 상태 |
|---|---|---|
| E11 | 세션별 역할 부여 방식 | **확정** — 파일 3층 + 포인터 주입. 페르소나 분리까지 승인됨 |
| E1' | 세션 격리 — repo 공유 vs 세션별 워크트리 | **잠정: 1차 공유 + 워크트리 옵션** — 사용자 확정 필요 |
| E12 | 임무를 브랜치에 강제 연결할 것인가 | 권장: 연결하되 강제는 아님 |
| W1 | 워크스페이스 레지스트리 저장 위치·등록 방식 | 권장: 앱 데이터 JSON, 등록 무제한 |
| W2 | git 패널이 보는 대상 | 권장: 활성 세션의 워크트리 + repo 전체 토글 |
| W3 | `team.json`을 repo 안에 둘 것인가 | 권장: `.eqmux/team.json` — 커밋하면 사람 팀원끼리 편성 공유 |
| D11 | 실행 중 에이전트 총량 경고·상한 | 신규. 열림 상한(10)과 분리 필요 |
| PRD C 미해결 1 | 스크롤백 SGR 저장 형식 | 프로토타입 용량 비교 후 |
| PRD C 미해결 2 | 셸 통합 없는 세션은 명령 경계 불가 | A6과 함께 |
| PRD C 미해결 3 | 워크스페이스별 DB 분리 여부 | **W0 확정으로 재검토 — repo별 분리가 자연스러워짐** |

---

## 6. 다음 단계

1. **T8 역할 설계 확정** — E11을 닫으면 D5(권한 모드)·E6(페르소나 매핑)도 함께 답이 나온다
2. **E1'·E12·W1·W2·W3·D11 확정**
3. **§4 문서 반영** — 한 번에 정리 (개별 반영하면 두 번 고치게 됨)
4. **PRD 작성 재개** — 순서상 A(터미널 코어)이나,
   워크스페이스 모델이 크게 바뀌었으므로 **E(팀 & 임무)를 먼저 쓰는 편이 나을 수 있음**

### PRD 작성 순서 (원안)

C → A → D → B → E → G → F → I → H → J → K
*(C 완료. 다만 4번 항목의 이유로 E를 앞당기는 안 검토)*
