# BASELINE — AgentCommender 기준선 실측

> 작업: `S0-1` · 측정: 윤해원(Supporter) · 2026-08-04
> 대상: AgentCommender v0.1.0 (`Desktop\GitProject\ClaudeCodeTemplate\root\AgentCommender\ops`)
> **EQMUX는 이 문서의 "재현 절차"를 그대로 써서 재야 비교가 성립한다.**

---

## 0. 한 장 요약

| # | 항목 | 값 | 상태 |
| --- | --- | --- | --- |
| 1 | 콜드 스타트 (첫 프롬프트까지) | **1,531 ms** | ✅ 측정 |
| 1b | 콜드 스타트 (터미널 4개 전부 준비) | **3,186 ms** | ✅ 측정 |
| 2 | 유휴 RAM · 앱 전용 (터미널 4개) | **566.3 MB** | ✅ 측정 |
| 2b | 유휴 RAM · 프로세스 트리 전체 | **1,020.3 MB** | ✅ 측정 |
| 2c | └ 셸 페이로드 (EQMUX에도 그대로 존재) | **454.0 MB** | ✅ 측정 |
| 3 | 키 입력 지연 p99 | — | ⛔ **측정 불가 — §5** |
| 4 | 배포 산출물 (dist) | **7.34 MB** | ✅ 측정 |
| 5 | 런타임 (node_modules) | **432.54 MB** | ✅ 측정 |
| 5b | └ electron/dist 단독 | **347.39 MB** | ✅ 측정 |
| 6 | 앱 데이터 (Electron userData) | **46.5 MB** | ⚠️ **브리프와 불일치 — §6** |
| 7 | 소스 규모 | 44파일 / 11,095줄 (TS·TSX) | ✅ 측정 |

**세 값 중 두 개가 나왔고 하나는 막혔습니다.** 막힌 이유와 해제 조건은 §5에 있습니다.

---

## 1. 측정 환경

| 항목 | 값 |
| --- | --- |
| OS | Windows 11 Home 10.0.22631 |
| 측정 일시 | 2026-08-04 15:42 ~ 15:50 KST |
| 셸 | `pwsh.exe` (WindowsApps 7.6.4.0) — 라이브 설정과 동일 |
| 앱 버전 | `agent-commender` 0.1.0 |
| 하니스 | `scratchpad/baseline-run2.ps1` (전문은 §7) |

### 격리 방식 — 라이브 인스턴스를 건드리지 않았다

측정 시점에 AgentCommender가 **팀 4개(Administrator·WordDoc·NovelTeam·EduCraft) · 세션 10개**를 물고
실행 중이었다. 이 문서를 쓰는 세션도 그 안에 있다(`pwsh ← claude.exe ← pwsh ← electron.exe`).

**앱을 재시작하면 살아 있는 세션이 전부 죽으므로, 별도 인스턴스를 띄워 측정했다.**

```
--user-data-dir=<임시>          Electron 단일 인스턴스 락 회피 (락은 userData 경로 기준)
ACMUX_STATE_PATH=<임시>/state.json    라이브 state.json 미접촉
ACMUX_WORKSPACE_ROOT=<임시>/ws        라이브 워크스페이스 미접촉
```

→ 라이브 인스턴스(PID 24388)와 상태·워크스페이스·userData가 완전히 분리된 채로 측정됐다.
측정 후 격리 인스턴스와 그 자손 프로세스를 전부 종료했다.

> **이 방법은 EQMUX에도 그대로 필요하다.** 도그푸딩(관문 B) 중에는 EQMUX도 팀 세션을 물고 있을 것이므로,
> 재측정 때 같은 격리 스위치가 없으면 측정 자체가 파괴적 행위가 된다. **S1 단계에서 세아에게 요청한다.**

---

## 2. 콜드 스타트 — 1,531 ms

### 정의

> **T0** = 앱 프로세스 생성 시각 (`Get-Process.StartTime`, 커널 기록값)
> **T1** = 첫 세션의 `session.log`에 첫 바이트가 기록된 시각 = **셸 프롬프트가 그려진 시점**

### 결과

| 구간 | 값 |
| --- | --- |
| T0 → T1 (첫 프롬프트) | **1,531 ms** |
| T0 → 터미널 4개 전부 준비 | **3,186 ms** |

- 세션 4개를 미리 담은 `state.json`을 복원시키는 조건이다(사용자가 앱을 켜면 마주하는 실제 상황).
- 팀 모드는 `terminal` — 규약 파일 스캐폴드가 없어 파일 I/O가 최소인 조건이다. **가장 유리한 값**이다.
  `persona` 모드는 `ROLE.md`·`CLAUDE.md`·`Persona/` 스캐폴드가 붙으므로 더 느리다(미측정).

### 이 값의 한계 — 반드시 같이 읽을 것

1. **"입력 가능"이 아니라 "프롬프트 출력"이다.** 브리프의 정의는 *"첫 프롬프트 입력 가능까지"*인데,
   외부에서 "입력을 받을 준비가 됐다"를 관측할 방법이 없다. 프롬프트 출력은 그 직전이며 **하한값**이다.
2. **pwsh 프로필 로딩이 포함돼 있다.** 셸 자체의 기동 시간이 섞여 있다. EQMUX도 같은 셸을 쓰므로
   **비교에는 문제없지만**, "앱이 1.5초를 쓴다"로 읽으면 안 된다.
3. **1회 측정이다.** 분산을 모른다. 관문 A 재측정 때는 10회 반복 후 중앙값·p95를 쓴다.

### 재현 절차

```powershell
# 1) 격리 환경 준비
$env:ACMUX_STATE_PATH     = "<임시>/state.json"   # 팀1 × 세션4, mode=terminal 로 시드
$env:ACMUX_WORKSPACE_ROOT = "<임시>/ws"
# 2) 기동
$p  = Start-Process <electron.exe> -ArgumentList '<ops>','--user-data-dir="<임시>/udata"' -PassThru
$t0 = (Get-Process -Id $p.Id).StartTime
# 3) 20ms 폴링으로 <ws>/BASE/t1/session.log 의 Length > 0 시각 포착 → T1
# 4) 콜드스타트 = T1 - T0
```

**EQMUX 측정 시**: EQMUX가 세션 로그를 안 쓴다면 등가 신호를 하나 정해야 한다
(권장: PTY의 첫 출력 바이트를 받은 시각을 앱이 stderr/로그로 1줄 찍게 하는 측정 전용 플래그).
**신호를 바꾸면 두 숫자는 비교 불가다.**

---

## 3. 유휴 RAM — 앱 566.3 MB / 트리 1,020.3 MB

### 조건

터미널 4개 · 아무 명령도 입력하지 않음 · 기동 후 방치. **관제 대시보드가 열린 기본 상태.**

### 시계열 (WorkingSet64 합)

| 경과 | 트리 전체 | proc | electron | pwsh | conhost | tasklist |
| --- | --- | --- | --- | --- | --- | --- |
| 34s | 953.1 MB | 14 | 4개 / 576.5 MB | 4개 / 326.7 MB | 5 | 1 |
| 64s | 1,034.1 MB | 16 | 4개 / 571.7 MB | 4개 / 325.7 MB | 6 | 1 |
| 95s | 1,039.9 MB | 22 | 4개 / 566.5 MB | 4개 / 337.9 MB | 9 | 5 |

### 최종 분해 (95초 시점)

| 구성 | 개수 | RAM | EQMUX에서 사라지나? |
| --- | --- | --- | --- |
| **electron** (앱) | 4 | **566.3 MB** | **사라진다** → Tauri/WebView2로 대체 |
| pwsh (셸) | 4 | 337.9 MB | **안 사라진다** — 같은 셸을 띄운다 |
| conhost | 8 | 72.8 MB | 안 사라진다 — ConPTY 부속 |
| tasklist | 4 | 41.1 MB | 설계하기 나름 — §3.2 |
| powershell | 1 | 2.2 MB | 안 사라진다 |
| **트리 전체** | **21** | **1,020.3 MB** | — |
| **└ 셸 페이로드(앱 제외)** | 17 | **454.0 MB** | **그대로 남는다** |

### 3.1 ⚠️ 관문 A-4가 지금 정의로는 통과 불가능하다

WORKPLAN 관문 A-4: **"유휴 RAM — 기준선(S0-1) 대비 40% 이하"**
브리프 S0-1 정의: **"터미널 4개 띄우고 10분 방치 후 전 프로세스 WorkingSet 합"** = **트리 기준**

트리 기준으로 계산하면:

```
목표치 = 1,020.3 MB × 0.4 = 408.1 MB
그런데 EQMUX에도 그대로 존재하는 셸 페이로드 = 454.0 MB

454.0 > 408.1  →  EQMUX 앱이 0 MB여도 관문 A-4는 미통과
```

**앱을 아예 안 만들어도 실패하는 기준입니다.** pwsh·conhost는 EQMUX가 띄워도 똑같이 뜹니다.

**제안 — A-4를 앱 전용 RAM으로 재정의**

| | 현재 (트리) | 제안 (앱 전용) |
| --- | --- | --- |
| 기준선 | 1,020.3 MB | **566.3 MB** |
| 40% 목표 | 408.1 MB (달성 불가) | **226.5 MB** |
| 측정 대상 | 자손 프로세스 전부 | EQMUX 실행 파일 + WebView2 프로세스군만 |

트리 값도 계속 기록하되 **판정은 앱 전용으로** 한다. 트리 값은 "사용자가 체감하는 총량"이라
버릴 이유는 없지만, 재작성이 개선할 수 없는 부분을 판정에 넣으면 안 된다.

> 이건 제 제안이고 **판정은 이안 몫**입니다. `issue.md #6`으로 올렸습니다.

### 3.2 관찰 — 유휴 상태인데 프로세스를 계속 스폰한다

`conhost` 5→6→9개, `tasklist` 1→1→5개로 변동한다. 단조 증가는 아니므로 **누수는 아니고**,
짧게 살다 죽는 프로세스를 표본 시점에 잡은 것으로 보인다.

원인 추정: **관제 대시보드가 5초 주기로 포트를 폴링**한다(`Tech.md` FS-5 하단 요약 · FS-11).
`netstat -ano` + `tasklist` + `Get-CimInstance Win32_Process`를 매번 `execFile`로 부른다.

- **아무 일도 안 하는 앱이 5초마다 프로세스 3개 + conhost를 만든다.**
- 확정은 아니다 — 코드 확인이 필요하다(제 권한 밖: 구현은 세아).
- **EQMUX 설계 메모**: Rust는 이걸 프로세스 스폰 없이 직접 조회할 수 있다(`netstat` 대신 IP Helper API,
  `tasklist` 대신 프로세스 열거). 그대로 이식하지 않는 게 좋다.

### 3.3 이 값의 한계

- **방치 시간이 95초다. 브리프 규격은 10분이다.** 34s→95s 구간에서 트리 값이 953→1,040MB로
  아직 오르는 중이었다. **10분 값은 이보다 높을 가능성이 크다.** 재측정 필요 — 대신 격리 인스턴스를
  10분 띄워둬야 하므로 그 시간만큼 창이 떠 있는다.
- 1회 측정이다.
- `WorkingSet64` 기준이다(공유 페이지 중복 계산 포함). Private Bytes 기준이면 값이 달라진다.
  **어느 쪽을 쓰든 EQMUX도 같은 지표로 재야 한다.**

---

## 4. 디스크 — 브리프 수치 검증 결과

| 항목 | 브리프 값 | 실측 | 판정 |
| --- | --- | --- | --- |
| 자체 코드 (dist) | 7.3 MB | **7.34 MB** | ✅ 일치 |
| 런타임 (node_modules) | 432.5 MB | **432.54 MB** | ✅ 일치 |
| └ electron/dist 단독 | — | 347.39 MB | (신규) 런타임의 80.3% |
| 소스 규모 | 44파일 / 11,802줄 | 44파일 / **11,095줄** | ⚠️ 707줄 차이 |
| 앱 데이터 | ~0 MB | **46.5 MB** | ❌ **불일치 — §6** |

- **핵심 논거는 그대로 성립한다**: 우리 코드 7.34MB, 런타임 432.54MB. 최적화 대상은 런타임이 맞다.
- 소스 707줄 차이는 계수 방법 차이로 보인다(제 계수: `ops/src/**/*.{ts,tsx}`만).
  `build.mjs`·설정 파일을 포함하면 메워질 수 있다. **논지에 영향 없음.**
- ⚠️ `Tech.md:4`는 같은 코드를 "~4,900행"이라고 적고 있다. 세 번째 값이다. → `issue.md #5`

---

## 5. ⛔ 키 입력 지연 — 이 방법으로는 측정 불가

**측정하지 못했습니다. 추정치를 적지 않겠습니다.**

### 왜 막혔나

관문 A-3의 기준은 **p99 ≤ 16 ms**다. 16ms는 60Hz 기준 **한 프레임**이다.

- 외부(PowerShell·CLI)에서 재면 프로세스 기동 오버헤드가 수십~수백 ms라 **측정 대상보다 잡음이 크다.**
- `acmux send` → `session.log` 왕복으로 재면 **xterm 렌더링 구간이 통째로 빠진다.** 그 구간이 바로
  WebGL이 걸린 자리이고, S1-3의 관심사다. 빼고 재면 의미가 없다.
- 고속 카메라·하드웨어 계측은 이 환경에 없다.

### 해제 조건 — 앱 안에 계측기가 있어야 한다

키 입력 지연은 **두 앱 모두에 같은 계측기가 들어가야** 비교가 성립한다.

```
keydown 이벤트 타임스탬프 (performance.now)
   → xterm term.write() 직후
   → 렌더 완료 콜백 (WebGL 프레임 present / requestAnimationFrame)
= 이 구간을 앱이 직접 기록해 p50/p95/p99로 뱉는다
```

**요청 (→ 세아 · 이안 판단)**

1. **측정 전용 플래그**를 EQMUX S1 단계에 넣어 주십시오 — `--latency-probe` 같은 스위치로
   위 3점을 JSONL로 뱉게. **S1-3(WebGL 동작 확인)의 판정 근거가 바로 이 숫자입니다.**
2. AgentCommender 쪽 기준선은 **같은 계측기를 이식**해야 나옵니다.
   기능 동결 중이므로 **측정 코드 한정 예외**가 필요합니다 — 이안 판단 부탁드립니다.
3. 그 전까지 **관문 A-3은 판정 불가**입니다. 기준선이 없으면 "16ms 이하"는 통과 여부를 말할 수 있어도
   "AgentCommender보다 나아졌나"는 말할 수 없습니다.

> 대안이 하나 있습니다 — 개발자도구(F12)를 열어 렌더러에 계측 스니펫을 붙이는 방식.
> GUI 조작이 필요해 제가 단독으로 못 합니다. **필요하면 지시해 주십시오.**

---

## 6. ❌ 브리프 수치 정정 — "앱 데이터 ~0 MB"

브리프 §1과 `OBJECTIVES.md:37`의 표:

| | AgentCommender | wmux 0.13.0 |
| --- | --- | --- |
| 앱 데이터 | ~0 MB | 512 MB |

**대조군이 어긋나 있습니다.**

| 실제로 잰 것 | 경로 | 크기 |
| --- | --- | --- |
| 브리프의 "~0 MB" | `~\AgentCommender\` (state.json 하나) | 0.003 MB |
| **같은 성격의 대조군** | `%APPDATA%\agent-commender\` (Electron userData) | **46.5 MB** |
| wmux | `%APPDATA%\wmux\` | **497.6 MB** |

`~\AgentCommender\`는 우리가 만든 상태 파일 폴더이고, wmux의 512MB는 **Electron userData 전체**다.
같은 층을 비교하면 **46.5 MB vs 497.6 MB**다.

### 그래서 결론이 바뀌나 — 부분적으로 바뀝니다

**우위는 유지된다** (10.7배). 하지만 **원인 해석이 달라진다.**

wmux 497.6 MB의 구성:

| 항목 | 크기 | 비중 |
| --- | --- | --- |
| Code Cache | 281.7 MB | 56.6% |
| Cache | 194.1 MB | 39.0% |
| Dictionaries | 10.9 MB | 2.2% |
| GPUCache | 5.6 MB | 1.1% |
| **캐시 계열 합** | **475.8 MB** | **95.6%** |

AgentCommender 46.5 MB도 구성이 같다 — Cache 20.3 · Code Cache 8.3 · Dictionaries 10.9 · GPUCache 5.6.

> **wmux의 512MB는 wmux의 결함이 아니라 Chromium 캐시가 상한 없이 자란 결과다.**
> AgentCommender는 같은 메커니즘을 갖고 있고, 단지 **덜 자랐을 뿐이다**(설치가 더 최근, 브라우저 패널 사용이 적음).

### EQMUX에 주는 함의 — 이게 제일 중요합니다

**Tauri/WebView2로 가도 이 문제는 안 사라집니다. WebView2는 Chromium입니다.**

- Tauri 전환이 없애는 것: **배포물에 실린 런타임 347MB**
- Tauri 전환이 **안** 없애는 것: **실행 중 쌓이는 브라우저 캐시**

`OBJECTIVES.md:48`의 *"성능 최적화는 런타임을 바꾸는 단 한 번의 결정"* 은 **배포 크기에 대해서만 참**입니다.
앱 데이터 증가는 별개 문제이고, **`S3-6`(스크롤백·디스크 상한 강제)이 그 유일한 방어선**입니다.

`WORKPLAN.md:119`가 *"wmux가 512MB가 된 지점이 정확히 여기"* 라고 적은 건 맞는데,
그 항목이 지금 **S3**에 있습니다. 캐시는 1차 도그푸딩 2주(관문 B) 동안에도 자랍니다.
→ `issue.md #7`로 올렸습니다.

---

## 7. 재현 — 하니스 위치

| 파일 | 용도 |
| --- | --- |
| `scratchpad/baseline-run2.ps1` | 콜드스타트 + 유휴RAM 시계열 (이 문서의 §2·§3 값) |
| `scratchpad/baseline-run.ps1` | v1 — 폴링 버그로 콜드스타트 미포착. **쓰지 말 것** |

하니스는 스크래치패드에 있어 세션이 끝나면 사라집니다.
**계속 쓸 값이면 `EQMUX/tools/baseline.ps1`로 옮겨야 합니다** — 옮길지는 이안 판단.
(작업 규칙상 제가 EQMUX 저장소에 코드를 두지 않았습니다.)

---

## 8. 남은 것

| # | 항목 | 막힌 이유 | 필요한 것 |
| --- | --- | --- | --- |
| 1 | 키 입력 지연 p99 | 앱 내부 계측기 없음 | 측정 플래그 (§5) — **세아·이안** |
| 2 | 유휴 RAM 10분 규격 | 95초까지만 쟀음 | 격리 인스턴스 10분 유지 — 지시만 주시면 함 |
| 3 | 반복 측정 (n=10) | 1회 측정이라 분산 모름 | 관문 A 전에 수행 |
| 4 | `persona` 모드 콜드스타트 | `terminal` 모드만 쟀음 | 스캐폴드 비용 확인용 |
| 5 | wmux 런타임 실측 | 디스크만 쟀음 | wmux 기동 승인 (별도 창이 뜸) |
| 6 | wmux 크래시 10건/19일 | `Crashpad\reports` 경로 없음 | 원 출처 확인 — **이안** |

> 6번은 짚고 갑니다. `%APPDATA%\wmux\Crashpad\reports`가 존재하지 않아 **크래시 10건을 확인하지 못했습니다.**
> 브리프의 "크래시 0건 vs 10건/19일"은 제가 검증한 값이 아닙니다. 출처를 알려주시면 확인하겠습니다.
> (재작성 명분이 "무거워서"이지 "불안정해서"가 아니라고 브리프가 명시했으므로 **논지에는 영향 없습니다.**)
