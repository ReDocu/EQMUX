# DEV-STATUS — 개발 현황 리스트

> 작성: 서이안 · 2026-08-05 18:4x · **v2.0** (팀장님 요청 — 완료 / 진행 중 / 할 일 3분할로 재작성)
> 갱신: 진세아 · 2026-08-06 10:0x · **v2.1** — §7 절차로 실측만 다시 셌다. 판정·배정은 손대지 않았다.
> 근거: 저장소 실측(`git`·파일·빌드) + 각 작업 산출 문서. **문서 요약이 아니라 코드 대조다.**
>
> - 계획·완료 기준의 정본은 [WORKPLAN.md](WORKPLAN.md), 결정 안건은 [issue.md](issue.md), 신규 요청은 [BACKLOG.md](BACKLOG.md).
>   여기는 **그 계획의 현재 상태**다. 갱신 방법은 §7 — **손으로 세지 말 것.**
>
> 🔴 **v2.1 시점의 어긋남 — 숨기지 않고 적는다(§7 마지막 줄).** 밤사이 들어온 `S2-11`·`S2-12`·`S2-13`이
> **`WORKPLAN.md`에 없다.** 정본이 계획을 모르는 채 코드만 있는 상태다. 여기 §1·§3에 실측으로 적어 두되
> **계획 정본 반영은 이안 몫**이다 — 순서를 뒤집으면 정본이 둘이 된다.
> 같이: `BRIEF-2026-08-05-S2-11.md`가 [PRESET-S2-11.md](PRESET-S2-11.md) 머리말에서 참조되는데 **저장소에 없다**(미커밋).

---

## 0. 한눈에

**1차(터미널 모드) · 관문 A 통과 · S2 진행 중 · 관문 A2 재도전 직전.**
창·분할·드래그·포커스·줌이 실제로 돌고, **유휴 메모리 정책까지 붙었다.**

| 지표 | 값 |
|---|---|
| 완료 | **21건** (S0 6 · S1 7 · S2 4 · 관문/판정 2 · 설계·진단 2) — v2.0 이후 증가 없음(아래 대기열로 감) |
| 검증 대기 | **8건** — 기존 3(`S2-3`·`S2-4`·`S2-10`) + 밤사이 5(`S2-11`·`S2-13`·`S2-5`·페인헤더·디자인이식) |
| 남은 1차 | **20건** (S2 5 · S3 8 · S4 6 · 관문 A2 재판정 1) — `S2-5` 구현이 대기열로 이동 |
| 제품 코드 | **11,061줄** — TypeScript 5,894 / Rust 5,167 (**+68%**, v2.0 대비) |
| 문서 | **46종** (`docs/`) + 측정 하니스 13종 (`tools/perf/`) |
| 빌드 | ⚠️ **확정 빌드 없음.** 08-05 18:25 exe(소스 `71ca09b`)는 그 뒤 커밋이 얹혀 무효. 08-06 10:34 exe(5,348,864 B)는 **디자인 WIP가 섞여** 확정 자격이 없다(§2 끝) |
| 커밋 | **51건** (`4e2139e` `a2b0d5f` 추가) · 원격 미반영 2건 |
| 결정 안건 | 19건 — **확정 18 · 진행 1**(`#19`) |
| 관문 A | ✅ **통과** (A-1·A-2·A-3) |
| 관문 A2 | ❌ **1차 미달** (482 vs 219.8) → `#19` 경로 확정 → **재도전 대기** |

**한 문장.** 관문 A2가 한 번 닫혔지만 **길이 났다** — `S2-10` 유휴 정책 실측에서
10분 시점 **≈175.7 MB**(참고치)가 나왔다. 판정선 **219.8 아래**다. 남은 건 규격대로 다시 재는 것뿐이다.

---

## 1. ✅ 완료 — 21건

### S0. 준비 (6/6)

| ID | 작업 | 담당 | 산출물 |
|---|---|---|---|
| S0-1 | 기준선 실측 (콜드스타트·유휴RAM·키입력지연) | 해원 | [BASELINE.md](BASELINE.md) |
| S0-2 | 기능 대조표 101개 ↔ AgentCommender 코드 | 해원 | [FEATURE-DIFF.md](FEATURE-DIFF.md) |
| S0-3 | PRD 전제 수정 (wmux 클론 → AgentCommender 후속작) | 이안 | [PRD-EQMUX.md](PRD-EQMUX.md) |
| S0-4 | Tauri 스파이크 — WebView2·WebGL 확인 | 세아 | [SPIKE-S0-4.md](SPIKE-S0-4.md) |
| S0-5 | design.pen 열람 + 이관 계획 | 이안 | [DESIGN-NOTES.md](DESIGN-NOTES.md) |
| S0-6 | 저장소 초기화 | 세아 | repo 골격 · `.gitignore` |

### S1. Walking Skeleton (7/7) — **관문 A 통과**

| ID | 작업 | 담당 | 무엇이 생겼나 |
|---|---|---|---|
| S1-1 | Tauri 골격 + Rust 백엔드 구조 | 세아 | `lib.rs`·`config.rs`·`commands/` |
| S1-2 | `portable-pty`로 ConPTY 연결 | 이안 대행 | `pty.rs` — pwsh 기동 · UTF-8 조립 |
| S1-3 | xterm.js + WebGL 렌더 | 세아 | **WebGL2 하드웨어 경로 확인** |
| S1-4 | PTY ↔ 프런트 양방향 스트림 | 이안 대행 | 한글 왕복 통과 |
| S1-3b | 계측 4점 분리 (`work`/`wait`) | 세아 | `latency.ts` · **자가 검증 3항** |
| S1-3c | A-2 폭 계측 (`--font-probe`) | 세아 | `font.ts` — 어느 폰트가 그렸는지까지 판별 |
| S1-5·6 | 관문 A 검증 + 판정 | 해원 → 이안 | **통과** — [GATE-A.md](GATE-A.md) |

### S2. 멀티플렉싱 (4/12 완료)

| ID | 작업 | 담당 | 무엇이 생겼나 |
|---|---|---|---|
| S2-1b | 앱 데이터 폴더 크기 리포트 | 세아 | `appdata.rs` · 13.75 MB(캐시 93%) |
| S2-1 | 레이아웃 트리 모델 + 직렬화 | 세아 | `layout.rs` · 스키마 v1 · 테스트 16/16 |
| S2-2 | 패널 분할·닫기 (4분할) | 세아 | `panels.ts` · **PTY kill 누수 수정** |
| S2-7 | 단축키 **기본값 확정** (구현은 미착수) | 이안 | [KEYMAP-S2-7.md](KEYMAP-S2-7.md) — 줌 키 오류 정정 |

### 관문·판정·설계 (4건)

| 무엇 | 담당 | 결과 |
|---|---|---|
| **관문 A 판정** | 이안 | ✅ 통과 — [GATE-A.md](GATE-A.md) |
| **관문 A2 판정** | 이안 | ❌ 미달 482 vs 219.8 — [GATE-A2.md](GATE-A2.md) · `#19` |
| **SPIKE-A4** (유휴 RAM 482 분해) | 세아 | 바닥 380 · GPU 최대 · **레버 −88% 실증** — [SPIKE-A4.md](SPIKE-A4.md) |
| **S2-8 설계** (design.pen 토큰 매핑) | 이안 | [DESIGN-S2-8.md](DESIGN-S2-8.md) — 구현은 미착수 |

---

## 2. 🔵 진행 중 — 지금 이 순간

| 누가 | 무엇 | 상태 |
|---|---|---|
| **세아** | HEAD 재빌드 + 잔여 정리 | 🟢 **진행 중** — tsc ✅ · `cargo test` **41/41 ✅** · `design.pen` 커밋(`4e2139e`) · 빌드 중 |
| **해원** | **KR3** 콜드스타트 M2 재측정 | ⏳ **메모리 게이트(여유 ≥3GB) 대기** — 수동 트리거 |
| **이안** | 검증 대기열 관리 | ⏳ **빌드 대기** — 대기열이 넷에서 **여덟**으로 늘었다 |

### 검증 대기열 — 빌드 하나에 **여덟**이 얹혀 있다

**기준 빌드가 한 번 무효가 됐다.** 어제 저녁 대기열 넷은 소스 `71ca09b`에 묶여 있었는데,
그 뒤 `lib.rs`·`layout.rs`·`pty.rs`·`main.ts`를 바꾸는 5커밋이 얹혔다. 그 exe로 재면
HEAD가 아니라 어제 저녁을 재는 것이다 — **`ac9758a` 절차(커밋 해시·크기 대조)가 잡아내는 바로 그 상황이다.**
HEAD(`4e2139e`) 재빌드 뒤 **한 창에서** 처리한다. 창의 첫 동작은 여전히 **해시·크기 대조**다.

| # | 무엇 | 누가 | 남은 이유 |
|---|---|---|---|
| 1 | `S2-3` **드래그 육안** ("끊김 없이 따라옴") | 이안 (+팀장님 손) | 무인으로 못 잰다. 코드 리뷰는 통과 |
| 2 | `S2-4` **교차 검증 8항** (방향 전수·줌 왕복·키 누수) | 이안 | `#18` 규칙 2 — 구현자(해원)가 아닌 사람이 본다. 절차는 [FOCUS-S2-4.md](FOCUS-S2-4.md) §4-2 |
| 3 | `S2-4` **A-3 회귀** | 해원 | 조용한 창 필요 |
| 4 | `S2-10` **인수 판정** | 이안 | 실측 4항은 나왔다 — 판정만 |
| 5 | `S2-11` **교차 검증** (프리셋 6종 · 잎 신원 보존) | 이안 또는 해원 | 절차는 [PRESET-S2-11.md](PRESET-S2-11.md) §6. `--preset-probe`로 무인 확인 가능 |
| 6 | `S2-13` `ptyReport()` 경계 API | 미정 | 문서 없음 — `pty.rs` +253줄 |
| 7 | `S2-5` **탭(스키마 v2) 교차 검증** | 미정 | 🔴 **문서가 없다.** `layout.rs` 756줄 변경인데 검증자가 볼 절차서가 없다 |
| 8 | 페인 헤더 · 관제/터미널 디자인 이식 | 미정 | `styles.css` +1,502줄 · `dash.ts`·`statusbar.ts`·`layout-picker.ts` 신규. 육안 검증 필요 |

> 5~8은 **v2.0 이후 밤사이 들어온 것**이라 담당·절차가 아직 안 붙었다. 배정은 이안 몫.

### 재빌드 중 잡힌 것 — 하니스 결함 3건 (세아, 08-06 오전)

**셋 다 검증 경로 자체의 결함이다.** 제품이 아니라 "제품을 판정하는 도구"가 틀렸다.
**공통 원인 하나** — `S2-5`가 스키마를 v2로 내리면서 **그 위에 서 있던 불변식들을 같이 안 내렸다.**

| 무엇 | 증상 | 원인 |
|---|---|---|
| `--preset-probe` **거짓 미달** | 실제 상태(잎 4 · 탭 5)로 돌리면 **6종 전부 미달**(`pty_per_leaf`) · 종료 코드 1 | 불변식이 v1 시절 `PTY 수 == 잎 수`. `S2-5`가 **PTY를 잎에서 탭으로** 내리면서 깨졌다 |
| `--tab-probe` **거짓 미달** | 복원된 상태로 돌리면 `opened_to_target` 미달(**탭 4/3**) · 새 상태에선 3/3 통과 | 여는 횟수를 `i = 1`부터 세어 **"패널은 탭이 하나로 시작한다"** 를 암묵 전제로 깔았다 |
| `--tab-probe` 충돌 경고 **반대** | "`--pty-probe` 쪽만 돈다" → 실제로는 **탭 확인이 이긴다** | `S2-5`가 넣은 자리. `#20`과 **같은 종류의 결함**이다 |

격리해서 돌리면 프리셋 6종은 **전수 통과**한다(잎 8 · PTY 8 · 캔버스 16) — **엔진은 멀쩡하다.**
셋 다 고쳐서 이번 빌드에 넣었다. `#20`(경고문 정정)도 같이 반영했다 — 보류 사유였던
"확정 빌드 `71ca09b`와 어긋난다"가 **그 빌드가 이미 무효라 사라졌기 때문**이다.
지금 안 넣으면 오늘 만드는 빌드가 내일 같은 이유로 또 무효가 된다.

> **왜 기능 테스트가 못 잡았나.** `cargo test` 41/41은 전부 초록이었다. 세 결함은 전부
> **프런트 프로브의 판정식**에 있었고, 그건 단위 테스트가 없는 자리다. 거짓 *통과*가 아니라
> 거짓 *미달*이라 더 늦게 잡힌다 — 미달은 보이지만 "구현이 틀렸나" 부터 의심하게 만든다.
> 조건 격리(`#10`)를 먼저 안 했으면 멀쩡한 프리셋 엔진을 뜯을 뻔했다.

> ⚠️ **측정하는 사람에게**: `--preset-probe`·`--layout-probe`는 `EQMUX_STATE_PATH` 없이 돌리면
> **라이브 상태 파일을 건드린다.** 오늘 오전 내가 격리 없이 돌려서 그 파일이 한 번 쓰였다
> (내용 손실은 확인 안 됨 — 잎 4·탭 5 그대로). 격리 스위치를 먼저 걸 것.

### 🔴 확정 빌드는 아직 못 만든다 — 트리에 동시 작업이 있다

10:34 exe로 위 검증 3건을 돌렸지만 **그 exe는 확정 빌드가 아니다.** 빌드 시점에
`src/styles.css`(10:28)가 미커밋 상태로 섞여 들어갔고, `src/dash.ts`·`src/icons.ts`는
빌드 **뒤에도** 계속 바뀌고 있었다(10:34:24 · 10:35:45 · 10:36:12) — 다른 세션이
같은 트리에서 디자인을 작업 중이다.

판정 로직은 `a2b0d5f` 소스 그대로라 위 3건의 통과/미달 대조는 유효하다. 하지만
`ac9758a` 절차가 요구하는 **"깨끗한 트리에서 빌드"** 조건을 못 맞추므로 **검증 대기열
8건을 여기에 얹으면 안 된다.** 디자인 작업이 커밋되고 트리가 조용해진 뒤 다시 만든다.

> **오늘 이 함정에 두 번 걸렸다** — 어제 세 번 걸렸던 그 함정이다(`ac9758a`가 절차화한 것).
> 공유 트리에서 빌드는 시간이 아니라 **커밋 해시로만** 증명된다.

### 그 뒤에 곧바로 오는 것

**해원 A2 재측정** (`#6` 규격 · 여유 ≥3GB · 정책 켠 채 · **+유휴→첫키 +1h 안정성**) → **이안 재판정**.
통과하면 **관문 A2가 닫히고 S3 잠금이 풀린다.**

---

## 3. ⬜ 해야 할 것 — 21건

### S2 잔여 (6건)

| ID | 작업 | 담당 | 선행 |
|---|---|---|---|
| ~~S2-5~~ | ~~탭(서피스)~~ — **구현됨**(`dcb0114`) · 검증 대기 #7 · **문서 없음** | 세아(코어) | — |
| S2-6a | 워크스페이스 **모델** (직렬화 확장) | **세아**(코어) | S2-5 ✅ — **지금 열려 있다** |
| S2-6b | 워크스페이스 **사이드바 UI** | **해원**(풀) | S2-6a |
| S2-7 | 단축키 **구현** (기본값은 확정됨) | **이안**(풀) | S2-6b |
| S2-8 | UI를 design.pen에 맞추기 (설계는 완료) | **이안**(풀) | S2-6b |
| S2-9 | 교차 검증 (단축키 전수) | 해원˚ | S2-7 |

**밤사이 들어온 것 — `WORKPLAN.md`에 아직 없다** (배정 근거는 [PRESET-S2-11.md](PRESET-S2-11.md) §0 참조)

| ID | 작업 | 담당 | 상태 |
|---|---|---|---|
| S2-11 | 배치 프리셋 엔진 (6종 → 트리 · 잎 신원 보존) | 세아(코어) | 구현·자기검증 ✅ · 교차 검증 대기 |
| S2-12 | (프리셋 UI로 추정 — 문서·브리프 없음) | 해원 | ❓ 확인 필요 |
| S2-13 | 코어 경계 API `ptyReport()` | 이안 | 코어분 구현 ✅(`7e3147c`) · 문서 없음 |

### S3. 기본기 마감 (8건) — 🔒 **`#19` 잠금 중**

`S3-1` 복붙(이안) · `S3-2` 검색(해원) · `S3-3` 폰트·셸(해원) · `S3-4` 세션 영속(세아) ·
`S3-5` `config.toml`+`EQMUX_PIPE_NAME`(이안) · `S3-6` 상한 강제(세아) · `S3-7` 마우스(세아) · `S3-8` 회귀 검증(해원)

> **WORKPLAN A-4 원문 이행** — *"S3로 넘기지 않는다."* **`#19`가 닫혀야 S3가 열린다.**

### S4. 도그푸딩 (6건)

`S4-0`·`S4-1`·`S4-1b`·`S4-2`·`S4-3`·`S4-4`. 판정 기준은 하나: **"팀 중 누구도 AgentCommender로 돌아가지 않았다."**

### 관문 A2 재판정 (1건) — 이안

### 미결 — 팀장님 답이 필요한 것

| # | 무엇 | 급한가 |
|---|---|---|
| 1 | `BACKLOG` **B1**(폴더 탐색기 범위) · **B2**(세션 간 대화 정지 조건) | ❌ 2차·4차 — 1차 안 막음 |
| 2 | **F9(SSH)·F10(원격 관리)** 를 정말 버릴 것인가 | ⚠️ **S3 전까지** — IPC 설계가 달라진다 |
| 3 | UI 서체(Geist) **번들 여부** — 제 권고: 폴백(번들 안 함) | ❌ S2-8 구현 전까지 |
| 4 | `design.pen`에 `1차 — 터미널 뷰` 프레임이 없는 게 의도인지 | ❌ 확인만 |
| 5 | **EQMUX 앱 라이선스 미정** (폰트 OFL은 닫힘) | ⚠️ 공개 배포 전 |

˚ 해원 구현분의 교차 검증은 세아 또는 이안 (`#18` 규칙 2 — 구현자와 검증자를 분리한다)

---

## 4. 관문 현황

| 관문 | 항목 | 기준 | 실측 | 판정 |
|---|---|---|---|---|
| **A** | A-1 한글 IME | 조합 중 안 깨짐 | 육안 | ✅ |
| **A** | A-2 CJK 폭 | 한글 = ASCII의 2.0배 | **2.0000** (오차 0.000 px) | ✅ |
| **A** | A-3 키 입력 지연 | 실작업 p99 ≤8ms · 총지연 ≤2프레임 | M2 **0.8 / 8.7 ms** | ✅ **기계 둘에서** |
| **A2** | A-4 유휴 RAM | 앱 전용 **≤ 219.8 MB** | **482** (1차) → **≈175.7**(S2-10 참고치) | ❌ **미달 → 재도전** |
| **B** | 도그푸딩 | 아무도 옛 앱으로 안 돌아간다 | — | ⏳ S4 |

**A2가 왜 미달했나 — 전제가 절반 틀렸다.** 482의 95%가 WebView2(458)였고, 그중 78%가
**빈 화면 Chromium 상주**(바닥 380)다. *"Electron을 빼면 그만큼 빠진다"* 는 추정은 배포물(−99%)에선
맞았지만 유휴 RAM(−12%)에선 틀렸다. **WebView2도 Chromium이다.**

**왜 다시 해볼 만한가.** `MemoryUsageTargetLevel(Low)`이 그 상주를 −88% 걷어내는 것이 실증됐고
(기능·A-3 무회귀), `S2-10`이 그걸 **유휴에만** 거는 정책으로 만들었다. A-4가 재는 것이 정확히
"유휴 RAM"이므로 눈속임이 아니라 **관문이 재는 그 값을 실제로 고치는 제품 행동**이다.

> 🔴 **정직성 가드** (`#19` 결정 4). 재측정 인수 조건 둘 — ⓐ **유휴→첫 키**가 A-3 판정선 이내
> ⓑ Low 상태 **1h 안정성**. *유휴 숫자가 좋아지고 복귀가 나빠지면 그건 개선이 아니라 이동이다.*
> **이동을 통과로 세지 않는다.** (ⓐ는 세아 실측에서 3.7/4.2 ms로 이미 통과 — 재측정에서 재확인)

---

## 5. 코드 인벤토리

### 프런트 (TypeScript 5,894줄)

| 파일 | 줄 | 하는 일 |
|---|---:|---|
| `src/panels.ts` | 1,214 | 트리 → DOM · 패널 생명주기 · 분할선 드래그 · 줌 상태 · **탭 생명주기** |
| `src/main.ts` | 1,178 | 부팅·프로브 라우팅·상태줄·프리셋 배선 |
| `src/latency.ts` | 640 | 지연 4점 계측 · 자가 검증 · **유휴→첫키 계측** |
| `src/font.ts` | 544 | A-2 폭 계측 (픽셀 해시로 렌더 폰트 판별) |
| `src/dash.ts` | 505 | **관제 대시보드** (디자인 이식) |
| `src/pane-header.ts` | 336 | 페인 헤더 · 상태 4단계 · ⋯ 메뉴 |
| `src/layout-picker.ts` | 336 | **배치 프리셋 고르개** |
| `src/terminal.ts` | 312 | xterm 구성 · WebGL · 테마 24색 · `FONT_STACK` |
| `src/statusbar.ts` | 231 | 상태 표시줄 (디자인 이식) |
| `src/pty.ts` | 197 | PTY 배선 · `ptyReport()` |
| `src/focus.ts` | 175 | **방향 포커스 · 줌** (경계 API만 사용 · 해원) |
| `src/icons.ts` · `license.ts` · `log.ts` | 226 | 아이콘 · 라이선스 탑재 판정 · 로그 |

> `styles.css`는 위 줄 수에 안 들어간다 — 디자인 이식으로 **+1,502줄**.

### 백엔드 (Rust 5,167줄)

`layout.rs`(트리·직렬화·**스키마 v2 탭**) · `presets.rs`(**배치 프리셋 6종**) ·
`pty.rs`(ConPTY·UTF-8 조립·drop 스레드·**출력 바이트 계수**) · `probe.rs`(무인 측정) ·
`memlevel.rs`(**유휴 정책**) · `appdata.rs` · `lib.rs` · `config.rs` · `error.rs` · `commands/`(5파일)

### 무인 검증 CLI — 이게 관문 판정의 근거를 만든다

`--pty-probe` · `--latency-probe`(+`-inject-ms`·`-idle-wait`) · `--font-probe` · `--appdata-report` ·
`--panes`/`--panes-probe` · `--layout-probe` · `--memory-target` · `--memory-policy`(+`-idle`)

```powershell
npm install ; npm run tauri -- build    # ⚠️ cargo build --release 직접 사용 금지 (프런트가 컴파일 시점에 박힌다)
```

---

## 6. 순서 — 지금 막고 있는 것부터

| 순위 | 무엇 | 누가 | 왜 이것부터 |
|---|---|---|---|
| **1** | `S2-10` 커밋 → **재빌드** | 세아 | **검증 넷이 이 빌드 하나에 얹혀 있다** |
| **2** | 검증 창 (§2) — 육안 · 교차검증 · A-3 · 인수 | 이안 · 해원 | 창 하나면 넷이 끝난다 |
| **3** | **A2 재측정 → 재판정** | 해원 → 이안 | 통과하면 **S3 잠금 해제** |
| 4 | KR3 콜드스타트 (게이트 대기) | 해원 | 관문 안 막음 · 게이트만 열리면 |
| 5 | `S2-5` 탭 → `S2-6a` → `S2-6b` | 세아 · 해원 | S2 잔여 체인 |

> **팀장님께 필요한 것 하나**: 메모리 여유(≥3GB). KR3 발사와 검증 창이 둘 다 여기 걸려 있다.
> Chrome 탭·ChatGPT 정리가 가장 큰 몫이다.

---

## 7. 이 문서 갱신하기

```powershell
cd C:\Users\LEE\Desktop\workspace\Administrator\project\EQMUX
git log --oneline | Measure-Object -Line          # 커밋 수
git status --short                                # 진행 중 흔적
(Get-Content src\*.ts | Measure-Object -Line).Lines
(Get-Content src-tauri\src\*.rs,src-tauri\src\commands\*.rs | Measure-Object -Line).Lines
Get-ChildItem docs\*.md | Measure-Object
Get-ChildItem src-tauri\target\release\eqmux.exe | Select Name,Length,LastWriteTime
```

- 작업 상태는 `WORKPLAN.md`를 고친 **뒤에** 옮긴다. 반대로 하지 않는다 — 정본이 둘이 되면 갈린다.
- 관문 판정은 `GATE-A.md`·`GATE-A2.md`가 정본. 여기는 복사본이다.
- **코드와 문서가 어긋나면 숨기지 말고 적는다.** 어긋났다는 사실 자체가 정보다.
