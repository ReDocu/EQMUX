# EQMUX 성능 테스트 규격서

> 작성: 윤해원(Supporter) · 2026-08-05 · **v1.1** · 받는 사람: 서이안(Manager)
> **목적**: 성능 측정이 다섯 문서에 흩어져 있다. **무엇을 · 어디서 · 어떤 자로 재는지**를 한 장으로 묶는다.
> 원 측정치는 각 문서에 그대로 있고, 이 문서는 **규격과 절차**다.
>
> **v1.1 정정 (해원, 2026-08-05)** — 근거: [BRIEF-2026-08-05-SUPPORTER.md](BRIEF-2026-08-05-SUPPORTER.md) §2
> · [GATE-A.md](GATE-A.md) §0 · [issue.md](issue.md) `#15` · [MACHINE-SPEC.md](MACHINE-SPEC.md) §1-2
>
> **관문 A 통과**(A-1·A-2·A-3) · **판정 기계 M1 → M2**(주사율 120 Hz) · 배포물 4.93 MB ·
> 하니스 `tools/perf/` 이관 완료. **§1-1의 "M1 고정"을 `#15`의 규칙으로 교체**했다.
>
> ~~🔴 두 자리는 고치지 않고 §8에 올렸다 — 판정이 갈리는 자리라 내 몫이 아니다~~
> → ✅ **§8 셋 다 결정됨** (이안 · 2026-08-05 · [BRIEF-2026-08-05-KR3-M2.md](BRIEF-2026-08-05-KR3-M2.md)) —
> §8-1 KR3 **M2 재측정**(조건 셋) · §8-2 **30초 유지**(`#17`) · §8-3 **동일 기계 인정**(`#16`).
> *고치지 않고 올린 경로가 실제로 값을 했다* — 셋 중 하나(§8-1)가 지시의 오류를 잡았다.

---

## 0. 한 장 요약 — 지금 어디까지 왔나

**값에는 기계 코드를 붙인다** (`#10`). **판정 기계는 2026-08-05부터 `M2`다** (`#15` · §1-1).

| # | 항목 | 규격 | AgentCommender (기준선) | EQMUX | 관문 |
|---|---|---|---|---|---|
| P1 | **콜드 스타트** | n=10 · 중앙값/p95 | **476 ms** (p95 506) · **M1** | 미측정 | KR3 (`#14`) 🔴 §8-1 |
| P2 | **유휴 RAM (앱 전용)** | `#6` · 터미널 4개 · 10표본 중앙값 | **549.6 MB · M2** ✅ 재측정 완료 (M1 558.2 / M3 552.5는 기록) | 🔴 **482 MB · M2 — 미달 확정** (2.19배 · [GATE-A2.md](GATE-A2.md) `#19` · [RAM-A4.md](RAM-A4.md)) | **A2 🔴** |
| P3 | **앱 데이터** | `#7` · 빈 폴더 → 유휴 · **사용량 5항 동반** | **30.43 MB** · M1 | 13.70 MB (7초 · M1) · **10분 미측정** | B |
| P4 | **키 입력 지연** | `#10` · 실작업 p99 / 총지연 p99 | 측정 불가 (계측기 미이식 · `#9`) | **M1** 0.4 / 16.9 · **M2** 1.6 / 9.6 | **A-3 ✅** |
| P5 | **폰트 폭 비율** | CJK advance / ASCII advance = 2.0 | — | **2.0000 ✅** (오차 0.000 px) | **A-2 ✅** |
| P6 | 배포물 | 파일 크기 | 7.34 MB (dist) + 432.5 MB (런타임) | **4.93 MB** | KR1 |

**막혔던 것 둘 — 전부 풀렸다.** P5는 `#8` 동봉으로 **2.0000 + 육안까지 통과**([GATE-A.md](GATE-A.md) §1-2·§1-4),
P4는 `S1-3b` 인수로 실작업이 분리돼 **기계 둘에서 통과**했다(§2 P4).
**→ 관문 A 통과** (2026-08-05 · 이안 · 커밋 `7254e23`).

**빈 칸 ~~셋~~ 둘**: P1의 **EQMUX** 값 · P3의 EQMUX 10분 값.
(~~P2의 M2 기준선~~ → ✅ §2 P2 · ~~P2의 EQMUX 값~~ → ✅ **482 MB** — 🔴 **미달 판정** `#19` · [RAM-A4.md](RAM-A4.md))

> ✅ **A-4 판정선이 다시 섰다 — `219.8 MB`** (M2 기준선 549.6 × 40%). ~~확정은 이안 몫이다~~ → **확정됐다** (`#6` 갱신 · 08-05).
> ~~🔴 KR3(P1)에는 같은 문제가 남아 있다 — 기준선 476 ms가 M1 값이다. `#15`가 안 다뤘다.~~
> → ✅ **`#15` 할 일 4로 확장·결정됨** — M2 재측정, 조건 셋(§8-1). 측정 후 P1을 갱신한다.

---

## 1. 공통 규칙 — 이걸 어기면 숫자가 서로 비교가 안 된다

### 1-1. ~~측정 기계는 M1 하나로 고정한다~~ → **기준선과 판정 대상을 같은 기계에서 잰다** (`#15`)

**고정할 것은 기계가 아니라 규칙이다.** 기계가 또 바뀌어도 이 규칙은 안 바뀐다.

| 코드 | 기계 | 지금 | 주사율 | RAM |
|---|---|---|---|---|
| **M2** | `DESKTOP-SQ0OEKB` · ASUS Vivobook S 15 M5506UA · Ryzen 7 8845HS / Radeon 780M · Win 11 Home 10.0.22631 | ✅ **판정 기계** (2026-08-05~) · 세션이 여기서 돈다 | **120 Hz** | **15.3 GB** |
| M1 | `DESKTOP-8GPGUF8` · i7-14700KF / RTX 4070 Ti SUPER · Win 11 Pro 26200.8973 | 옛 판정 기계 · **접근 불가** | 60 Hz | 63.8 GB |
| ~~M3~~ | `BASELINE.md`(S0-1) 측정 기계 | 🔴 **M2와 같은 기계로 보인다 — §8-3** | 〃 | 〃 |

> **재현할 수 없는 값으로 관문을 닫지 않는다.** 인용할 때는 **기계 코드를 반드시 붙인다.**
> M1 값은 **기록으로 유효하고 판정 근거로는 못 쓴다.**
>
> 🔴 **단, M3에 대해서는 전제가 뒤집혔다** — 실측 대조 결과 **M3 = M2일 가능성이 매우 높다**(§8-3).
> 사실이면 `BASELINE.md`의 M3 값들은 *"재현 불가"* 가 아니라 **지금 여기서 다시 잴 수 있는 값**이 된다.
>
> ⚠️ **기계가 바뀌면 항목마다 "재현이면 되는가, 재측정이 필요한가"를 가른다** (`#15`).
> **A-3는 재현으로 끝났다** — 계측기가 판정선을 실측 주사율에서 스스로 파생하기 때문이다.
> **A-4는 재측정이 필요하다** — 판정선이 *다른 앱을 잰 기준선*에서 나오고, 그 기준선이 M1 값이다.
>
> ⚠️ **M1 → M2는 RAM이 4배 줄었다**(63.8 → 15.3 GB). `BASELINE.md` §3.5의
> *"기계를 바꿔도 유휴 RAM은 1.0%밖에 안 움직인다"* 를 근거로 재측정을 건너뛰지 않는다 —
> 그 1.0%는 **둘 다 메모리가 넉넉했던** M3 → M1 비교다.
> Windows는 압력이 걸리면 워킹셋을 잘라낸다. **어느 방향인지는 재 보기 전에는 모른다.**

### 1-1b. 주사율은 고정하지 않는다 — **판정 기계 실측값에서 파생한다** (`#15`가 `#13`을 대체)

A-3-②의 *"총지연 p99 ≤ 2프레임"* 은 기계마다 다른 값이 된다.

| 기계 | 실측 주사율 | 1프레임 | **A-3-② 판정선** |
|---|---|---|---|
| M1 | 59.9 Hz (rAF p50 16.7 ms) | 16.67 ms | 33.4 ms |
| **M2** | **120.5 Hz** (계측기 독립 추정) | 8.3 ms | **16.6 ms** |

> **주사율이 오르면 판정이 엄해진다.** M2로 옮기면서 판정선이 절반이 됐고 **그 상태로 통과**했다.
> `#13`(60 Hz 고정)의 논거는 *"기존 측정과 비교가 끊긴다"* 였는데, 비교가 끊기는 대신
> **판정이 강해졌다.** §1-4를 지킨 결과가 이쪽이다.

### 1-2. 격리 규칙 — 라이브를 죽이지 않고 재는 법

AgentCommender를 재려면 **별도 인스턴스**를 띄운다. 상태·워크스페이스·캐시를 전부 분리한다.

```
--user-data-dir=<임시>              Electron 단일 인스턴스 락 회피 (락은 userData 기준)
ACMUX_STATE_PATH=<임시>/state.json  라이브 state.json 미접촉
ACMUX_WORKSPACE_ROOT=<임시>/ws      라이브 워크스페이스 미접촉
```

**환경변수로 분리되지 않는 것이 하나 있다 — 명명 파이프** (`\\.\pipe\acmux` 하드코딩).
그래서 순서 규칙이 있다:

| 규칙 | 내용 |
|---|---|
| 1 | 격리 인스턴스는 **라이브가 이미 떠 있을 때만** 띄운다 |
| 2 | 격리가 떠 있는 동안 **라이브를 재시작하지 않는다** |
| 3 | 측정 **전후로 `acmux list`** 를 찍어 라이브 세션이 응답하는지 확인한다 |
| 4 | 격리 인스턴스를 쓰기 전에 **이안에게 알린다** |
| 5 | 끝나면 격리 프로세스가 **0개**인지 확인한다 |

> EQMUX는 이 제약을 물려받지 않게 **제어 채널 이름을 환경변수로** 덮어쓸 수 있어야 한다
> (`EQMUX_PIPE_NAME`). 없으면 도그푸딩(관문 B) 중 측정이 **파괴적 행위**가 된다. → 세아

### 1-3. 지표 정의를 바꾸면 그 전 값과 비교하지 않는다

| 무엇 | 이 프로젝트가 쓰는 값 |
|---|---|
| RAM | **`WorkingSet64`** (공유 페이지 중복 포함). Private Bytes로 바꾸면 전부 다시 잰다 |
| 앱 데이터 | **캐시 계열 판정은 [APPDATA-S2-1b.md](APPDATA-S2-1b.md) §2-3 규칙** — 이름에 `cache` 포함 또는 `dictionaries`/`shared dictionary`/`service worker`/`blob_storage` |
| 지연 | `#10`의 3분할 — **①실작업(판정) ②총지연(판정) ③주사율·기계(기록 필수)** |

### 1-4. 조건을 유리하게 바꾸지 않는다 (`#12`)

> 판정을 유리하게 만드는 방향으로 규격을 바꾸는 건 판정이 아니다.
> **불리해지는 방향이어도 근거 없이 바꾸지 않는다.**

### 1-5. 검증·측정용 빌드는 **커밋 해시·exe 크기 대조부터** (이안 ③ · 2026-08-05)

**공유 트리에서는 빌드도 조작이다** — 미커밋 변경을 품은 exe가 조용히 만들어진다. 같은 날 세 번 잡혔다
(옛 프런트 박힌 바이너리 · `dist/`-cargo 불일치 · **S2-10 WIP 오염 빌드**). 절차:

1. 빌드 전 `git status` — 트리가 더러우면 **작업자 커밋을 기다린다**
2. 빌드 후 커밋 해시 + exe 크기·시각을 기록하고 측정 출력과 대조한다
3. 하니스 쪽 하한선: `_paths.ps1`의 **`Assert-CleanBuildForVerdict`** — 트리가 더러우면 측정 자체를 멈춘다
   (`a2-idle-ram-eqmux.ps1`에 걸려 있다. EQMUX를 재는 하니스를 새로 쓰면 같은 게이트를 건다)

---

## 2. 항목별 규격

### P1. 콜드 스타트

| | |
|---|---|
| **정의** | `T0` = 프로세스 생성 시각(`Get-Process.StartTime`, 커널값) → `T1` = **셸이 첫 출력을 낸 시각** |
| **조건** | 팀 1개 × 세션 4개(`t1`~`t4`) · `mode=terminal` · 상태 복원 |
| **표본** | **n=10** + 워밍업 1회(버림) · 중앙값·p95 보고 |
| **현재값** | M1 · AgentCommender — 첫 프롬프트 **476 ms** (p95 506) · 4개 준비 506 (p95 568) |
| **문서** | [BASELINE.md](BASELINE.md) §2.1 |

> ⚠️ **`T1`을 "session.log 첫 바이트"로 잡으면 틀린다.** 첫 바이트는 셸 출력이 아니라
> `pty-manager.ts:111`이 세션 생성 즉시 쓰는 헤더다. **두 값은 90 ms 차이 난다.**
> 헤더 줄 **뒤에** 처음 붙는 실내용을 봐야 한다.
>
> ⚠️ **이 값은 "웜 재기동"이다.** 연속 기동이라 디스크 캐시가 뜨겁다.
> 진짜 콜드(부팅 후 최초 1회)는 **재부팅당 1표본**밖에 못 얻는다. 필요하면 별도 지시.

**EQMUX 쪽 요구**: 세션 로그를 안 쓰면 **등가 신호**를 정해야 한다 —
권장은 *"PTY 첫 출력 바이트 수신 시각을 stderr에 1줄"* 찍는 측정 전용 플래그.
**신호를 바꾸면 두 숫자는 비교 불가다.**

### P2. 유휴 RAM

| | |
|---|---|
| **정의** | 프로세스 트리 전체의 `WorkingSet64` 합 · **앱 전용**은 앱 프로세스만 |
| **조건** | 터미널 **4개** · 아무 입력 없음 · `mode=terminal` |
| **표본** | `#6` — **안정화 30초 후 · 30초 간격 · 10표본 · 중앙값** |
| **현재값** | ✅ **M2** · AgentCommender — 앱 **549.6 MB** · 트리 851.3 · 셸 페이로드 301.8 · 진동 폭 25.4 |
| **기록** | M1 558.2 (§3.5) · M3 552.5 (§3) — **판정에 쓰지 않는다** |
| **판정** | A-4(관문 A2): EQMUX 앱 전용 ≤ **219.8 MB** (549.6 × 40% · `#6` 확정) — 🔴 **미달** (실측 **482 MB** · 이안 판정 · [GATE-A2.md](GATE-A2.md) `#19`). 앱의 95%가 WebView2(458 MB), 호스트는 24 MB. 목표는 안 낮춘다 — 세아 **SPIKE-A4** 진단 후 `#19`를 닫는다. 판정용 재측정 필요 시 `#6` 규격으로 해원 |
| **추가 기록** | 메모리 압력 — 시작 여유 3.16 GB(79.3%) → 종료 1.89 GB(87.6%) (`#15` 할 일 3) |
| **문서** | [BASELINE.md](BASELINE.md) **§3.6 (M2 · 판정용)** · §3.5 (M1) · [BRIEF-2026-08-05-A4-M2.md](BRIEF-2026-08-05-A4-M2.md) |

> ✅ **~~223.3 MB~~ → 219.8 MB.** `#15`의 M2 재측정을 마쳤다(2026-08-05 12:01~12:07).
> **기준선과 판정 대상이 다시 같은 기계에서 나온다.**
>
> 📌 **`#15`가 경고한 *"M2는 RAM이 4배 작으니 예측하지 말라"* 는 옳았고, 결과는 작은 이동이었다** —
> M1 대비 **−1.5%**, M3 대비 **−0.5%**. 예측하지 않고 잰 것이 맞았다.
>
> 🔴 **다만 진동 폭이 12.5 → 25.4 MB로 늘었다.** §8-2가 예고한 자리다.
> 원인은 분산이 아니라 **첫 표본 하나**다 — 90초 이후 구간의 폭은 **6.4 MB**로 M1보다도 작다.
> **안정화 30초가 M2에서는 부족하다.** 규격은 고치지 않고 관측 사실로 남겼다(§8-2 · `BASELINE.md` §3.6).

> ✅ **`#6`의 10표본 규격이 실제로 문제를 없앴다.** 단일 표본 시절 진동 폭이 86.5 MB로
> 목표치의 3배였는데(판정 기준으로 못 쓸 수준), 10표본 중앙값에서 **12.5 MB**로 떨어졌다.
>
> 📌 **RAM은 기계 성능으로 안 풀린다.** 같은 기계 교체에서 콜드스타트는 3.2배 움직였는데
> **앱 RAM은 1.0% 움직였다.** 디스크·CPU가 바꾸는 건 시간이지 상주 크기가 아니다.

### P3. 앱 데이터

| | |
|---|---|
| **정의** | 앱이 디스크에 쌓는 상태+캐시. **층을 맞추는 게 전부다** |
| **층** | AgentCommender = Electron `userData` **한 폴더** / EQMUX = `LOCALAPPDATA`(webview) **+** `APPDATA`(state) **합산** |
| **빼는 것** | 워크스페이스 · 재파스 포인트(링크·정션) · 겹치는 뿌리 |
| **현재값** | M1 · AgentCommender 빈 폴더 → 10분 유휴 **30.43 MB** (캐시 100%) |
| **문서** | [BASELINE.md](BASELINE.md) §3.2c · [APPDATA-S2-1b.md](APPDATA-S2-1b.md) §2·§3.5 |

> 📌 **"10분 유휴" 규격이 얻는 게 없다.** 7초에 30.43 MB에 도달한 뒤
> **600초까지 소수점 둘째 자리도 안 움직였다.** 캐시는 **페이지를 열 때** 자라고 유휴는 아무것도 안 연다.
>
> → **`#7`의 3회 측정이 의미 있는 이유는 "시간이 지나서"가 아니라 "그 사이에 썼기 때문"이다.**
> 측정 시점마다 **경과 시간이 아니라 사용량**을 같이 기록해야 비교가 선다. (→ `#7` 절차 보강 제안)
>
> 참고: 30.43 MB 중 **`Dictionaries` 10.94 MB는 안 써도 항상 붙는 Chromium 고정 자산**이다.
> wmux·AgentCommender 실측에서 **같은 값**으로 나온다.

### P4. 키 입력 지연

| | |
|---|---|
| **판정** | `#10` — ① 프레임 대기 제외 **실작업 p99 ≤ 8 ms** · ② 총지연 p99 **≤ 2프레임**. **프레임 길이는 판정 기계 실측 주사율에서 파생한다**(`#15` · §1-1b) — 고정값이 아니다 |
| **기록 필수** | rAF 간격 중앙값(실효 주사율) · GPU · PTY 연결 여부 · 표본 수 · **기계 코드** |
| **계측기 인수 기준** | [GATE-A.md](GATE-A.md) §2-2 **자가 검증 3항** — 부하 5 ms를 넣으면 `work_ms`가 5 ms 오르고, 프레임을 늦추면 `work_ms`는 안 변해야 한다. ✅ **세아 인수 완료** (2026-08-04 · 재실행 통과 · 결함 1건 수정) |
| **현재값** | 아래 표 — **기계 둘에서 통과** |
| **막혔던 것** | ~~A-3-①(실작업)은 아직 못 잰다~~ → ✅ **`S1-3b` 완료·인수.** 실작업이 분리됐다 |

**A-3 판정 — 두 기계 전부 통과** ([GATE-A.md](GATE-A.md) §0·§4-0 · `#15`)

| | M1 (59.9 Hz) | **M2 (120.5 Hz · 판정 기계)** | 상한 |
|---|---|---|---|
| 판정선 (2프레임) | 33.4 ms | **16.6 ms** | 실측 주사율에서 파생 |
| ① 실작업 p99 | **0.4 ms** ✅ | **1.6 ms** ✅ | ≤ 8 ms |
| ② 총지연 p99 | **16.9 ms** ✅ | **9.6 ms** ✅ | 2프레임 |

> **총지연이 오히려 내려갔다.** 프레임 대기가 반으로 줄어서다(`wait` p50 8.8 → 4.7 ms).
> 실작업은 0.4 → **1.6 ms로 4배 늘었다** — 약한 CPU가 그대로 드러난 값이고, 그래도 상한의 20%다.
> M2 측정 조건: `--latency-probe-run=500` · `verdict_valid: true` · **메모리 사용률 95%**(여유 0.7 GB).
>
> ⚠️ **옛 해석 하나가 무너졌다.** `RENDERER-S1-3.md`의 `render p99 16.3 ms`를
> *"60 Hz 한 프레임(16.67 ms)에 붙어 있다"* 로 읽은 문장은 **M2가 60 Hz라는 가정 위에 있었다.**
> M2의 한 프레임은 **8.3 ms**다 — 16.3 ms는 한 프레임이 아니라 **두 프레임**이었다.
> **판정은 안 바뀌지만 그 문장을 인용한 곳은 고쳐야 한다** ([MACHINE-SPEC.md](MACHINE-SPEC.md) §4).

> ⚠️ **순진하게 rAF 틱으로 자르면 회귀를 숨긴다.** 메인 스레드가 바쁘면 rAF 콜백 자체가 밀려서
> **느려질수록 `work`는 그대로이고 `wait`만 커진다.** 그래서 계측기부터 자가 검증을 통과해야 한다.
>
> AgentCommender 쪽 기준선은 **없다**(`#9` — 계측기 이식 안 함). 따라서
> *"AgentCommender보다 빠르다"* 는 **주장하지 않는다.** 절대 기준 단독으로 판정한다.

### P5. 폰트 폭 (A-2)

| | |
|---|---|
| **정의** | CJK advance width / ASCII advance width = **2.0** 이어야 한다 |
| **판정선** | 기기 픽셀 오차 **≥ 1.0 → 미달** (글리프가 제 칸을 한 픽셀 이상 벗어난다) |
| **현재값** | ~~1.7067 미달~~ → **2.0000 ✅** · 한글·漢字 2.0000 · `→`·`■` 1.0000 · 굵게 2.0000 · **전 항목 오차 0.000 px** |
| **대조군** | D2Coding 전부 제외 시 **여전히 1.7067 미달**(의도) — 동봉이 무엇을 막고 있는지가 숫자로 남았다 |
| **육안** | ✅ **통과** (2026-08-05 · 이안 · **M2** · DPR 2) — 6절 전부. 겹침·잘림·틈 없음 |
| **문서** | [FONT-A2.md](FONT-A2.md) §0-A · [GATE-A.md](GATE-A.md) §1-2·§1-4 |

> ⚠️ **원인이 기계가 아니라 산술이었다.** CJK advance는 사실상 모든 폰트에서 1.0em이므로
> **비율이 2가 되려면 ASCII advance가 정확히 0.5em이어야 한다.** Cascadia Mono는 0.586em이었다.
> **기계를 바꿔도 안 바뀌는 종류의 미달이었다.**
>
> ✅ **해결: 동봉** (`#8` 발동 · 이안 결정). `@font-face`로 실으면 **패밀리 이름을 우리가 정하므로**
> 설치 폰트 의존과 아래의 이름 불일치가 **함께** 사라진다. 배포물 3.21 → 4.93 MB.
>
> ⚠️ **측정 기계 전제가 하나 무너졌었다.** M1에 설치된 패밀리 이름은 `D2Coding ligature`인데
> 스택 첫 항목은 `"D2Coding"`이라 **한 번도 매칭된 적이 없었다.**
> → **M1은 이미 폴백이 도는 기계였다.** ~~→ `GATE-A.md` §1 갱신 필요~~
> → ✅ **갱신 완료** (이안, 2026-08-05). 옛 본문은 `GATE-A.md` §1-3에 기록으로 남았다.
>
> ⚠️ **폭 계측은 육안을 대체하지 않는다** (`FONT-A2.md` §6). 비율이 틀리면 볼 것도 없이 미달이고,
> 맞으면 **그때 눈으로 본다.** ~~지금은 전자다~~ → **후자까지 끝났다** (`GATE-A.md` §1-4).
> 동봉 덕에 설치 폰트에 의존하지 않으므로 **한글 코딩 폰트가 없는 M2에서 본 것이 더 엄한 조건이었다.**

---

## 3. 재현이 실패하는 실제 원인 — 겪은 것만

문서에 절차를 적어 두는 것과 그 절차가 실제로 도는 것은 다르다. **셋 다 직접 밟았다.**

| # | 함정 | 증상 | 고치는 법 |
|---|---|---|---|
| **1** | **열려 있는 파일을 독점 모드로 읽음** | 폴링이 **100% 실패**하는데 `catch`가 `false`를 돌려줘 **30초 타임아웃까지 조용하다** | `FileShare.ReadWrite \| Delete`로 연다. **감지 루프의 catch는 실패 횟수를 세서 출력한다** |
| **2** | **신호를 잘못 정함** | `session.log` 첫 바이트를 "프롬프트"로 읽었는데 실제로는 앱이 쓴 헤더 (90 ms 차이) | 신호가 **누가 언제 쓰는 값인지** 코드로 확인하고 정한다 |
| **3** | **기계가 같다고 가정** | 값이 3.2배 달라 *"내 방법이 틀렸나"* 부터 의심했다. 기계가 달랐다 | 측정 문서 **머리에 기계 코드**를 박는다. 값보다 먼저 |

> **3번 판별법**: 빌드 번호만 보면 *"업그레이드됐나 보다"* 로 넘어간다.
> **레지스트리 `InstallTime`과 에디션(Home/Pro)** 을 같이 본다. 에디션은 기능 업데이트로 안 바뀐다.

---

## 4. 관문별 현황

### ✅ 관문 A — **통과** (2026-08-05 · 이안 · 커밋 `7254e23` · [GATE-A.md](GATE-A.md) §0)

| 관문 | 항목 | 상태 |
|---|---|---|
| **A** | A-1 한글 IME | ✅ 육안 확인 (M1 · 팀장님) |
| | A-2 CJK 폭 | ✅ **통과** — 동봉 후 **2.0000**(오차 0.000 px) · **육안까지 통과**(M2 · DPR 2) |
| | A-2b `dir` 한글 파일명 | ✅ 확인 — 무인 검증에서도 정상 |
| | A-3 키 입력 지연 | ✅ **통과** — **기계 둘에서.** M1 0.4/16.9 · M2 1.6/9.6 (§2 P4) |
| **A2** | A-4 유휴 RAM | ✅ **기준선 M2 재측정 완료** — **549.6 MB** → 목표 **219.8 MB**(확정은 이안). 판정은 `S2-2` 후 |
| **B** | 캐시 3회 측정 | 규칙 확인 완료. ~~"사용량 기록" 추가 제안~~ → ✅ **`#7` ②에 반영됨** (*"매 회 사용량 5항 동반"*) |

> 🔴 **관문과 별개로 남은 것 하나 — OFL 1.1 전문 파일.** 관문 A는 렌더 품질 관문이라 라이선스가 막지 않지만
> **배포는 막는다.** ✅ 2026-08-05 세아 반영 확인 — `public/licenses/LICENSE-OFL-1.1.txt` ·
> `dist/licenses/` · `src/assets/fonts/NOTICE-D2Coding.md`.

---

## 5. 아직 없는 값

| # | 무엇 | 왜 없나 | 비용 |
|---|---|---|---|
| ~~0~~ | ~~AgentCommender 유휴 RAM — M2 기준선~~ | ✅ **완료** (2026-08-05 · `BASELINE.md` §3.6) | 실측 6분. 여유 3.16 GB에서 착수 |
| 1 | **EQMUX 콜드스타트** | 등가 신호 미정 | 세아가 측정 플래그 1줄 → 그 뒤 n=10은 5분 |
| ~~2~~ | ~~EQMUX 유휴 RAM~~ | ✅ **완료** (2026-08-05 · **482 MB · M2** · [RAM-A4.md](RAM-A4.md)) — 🔴 **미달 판정** (`#19`) | — |
| 3 | **EQMUX 앱 데이터 10분** | ~~`eqmux.exe`를 세아가 쓰는 중~~ → **육안이 끝나 관문을 안 막는다.** 세아의 `S2-1` **재빌드 창만 피하면 된다** | **창 하나 10분.** 절차는 `APPDATA-S2-1b.md` §4 |
| 4 | 진짜 콜드스타트(부팅 후 1회) | 재부팅당 1표본 | 지시 시 |
| 5 | `persona` 모드 콜드스타트 | `terminal`만 쟀음 | 스캐폴드 비용 확인용 |

> **0번이 1·2번보다 먼저다.** 판정선이 거기서 나온다. 다만 **착수 조건이 있다** —
> A-4는 AgentCommender에 터미널 4개를 띄우는 규격이고 M1에서 그게 **트리 895 MB**였다.
> M2 여유가 3 GB 아래면 스왑이 돌아 **재는 것이 앱이 아니라 메모리 압력**이 되고,
> 그 위에서 도는 세션들까지 같이 느려진다. **여유를 먼저 재고, 미달이면 멈추고 알린다.**
>
> ⚠️ **3번은 10분 내내 빌드·재시작이 없어야 한다.** 한 번이라도 들어가면 캐시가 리셋돼 10분이 통째로 날아간다.
> *"재빌드 끝난 뒤"* 로는 창이 안 잡힌다 — **세아와 구간을 직접 합의한다.**

> **3번은 예측이 있다** — AgentCommender가 7초 뒤 전혀 안 움직였으므로
> EQMUX의 `13.70 MB`(7초)도 10분 값과 같을 가능성이 높다. **예측이지 측정이 아니다.**

---

## 6. ✅ 하니스 — **`tools/perf/` 이관 완료** (2026-08-05)

~~스크래치패드에 있어 세션이 끝나면 사라진다~~ → **저장소 안으로 들어왔다.** 이안 승인 · 소유는 해원.

| 파일 | 항목 | 무엇을 재나 |
|---|---|---|
| `_paths.ps1` | — | **신설 (2026-08-05)** — 경로·기계 코드·메모리 압력을 실행 시점에 **실측으로** 해석 |
| `coldstart-n10.ps1` | **P1** | 격리 기동 ×11(워밍업 1 버림) · 공유 모드 폴링 |
| `a4-idle-ram-m1.ps1` | **P2** | `#6` 규격 10표본 · 프로세스 트리 BFS · **착수 전 메모리 여유 검사(3 GB)** |
| `appdata-idle10m-m1.ps1` | **P3** | AgentCommender — §2-3 캐시 규칙 · 재파스 제외 |
| `eqmux-idle10m.ps1` | **P3** | EQMUX — `LOCALAPPDATA` + `APPDATA` 합산 |

> ### 🔴 이관해 놓고 **아무도 안 돌려 봤다** — 넷 다 죽어 있었다 (2026-08-05 발견·수정 · 해원)
>
> | 무엇 | 상태 |
> |---|---|
> | **경로 하드코딩** | 넷 전부 M1 시절 `D:\...` 경로를 물고 있었다. **M2에는 `D:` 드라이브가 없다** — 첫 줄에서 죽는다 |
> | **기계 코드 하드코딩** | 결과 출력이 전부 `=== 결과 (M1 · …) ===`. **판정 기계는 M2인데 M1이라고 찍었다** |
>
> **둘째가 더 무섭다.** 경로는 죽어서 눈에 띄지만, 기계 코드는 **죽지 않고 조용히 틀린 값을 찍는다.**
> `#10`이 기계 코드를 붙이라고 한 이유는 값끼리 비교가 되게 하려는 것인데,
> **틀린 코드를 붙이면 비교가 되는 줄 알게 된다 — 없는 것보다 나쁘다.**
>
> → `_paths.ps1`로 고쳤다. 경로는 **라이브 프로세스의 실행 경로에서** 찾고,
> 기계 코드는 **호스트명 실측**으로 답한다. 못 찾으면 **추측하지 않고 멈춘다.**
> 5개 파일 문법 파싱·함수 동작 전부 확인했다 (실측: `M2 · DESKTOP-SQ0OEKB · 120 Hz · 15.26 GB`).
>
> **교훈은 하나다 — 이관은 파일을 옮기는 게 아니라 "다음 사람이 돌릴 수 있게 하는 것"이다.**
> 옮긴 날 한 번만 돌려 봤으면 그날 알았다.

**경계 4줄**은 [tools/perf/README.md](../tools/perf/README.md)에 있다. 요지는 셋이다 —
**제품 코드가 아니고**, 그래서 *"구현은 세아만"* 과 **충돌하지 않으며**(측정 도구는 재는 사람이 소유한다),
**제품 코드에 손이 필요해지면 거기서 멈추고 세아에게 넘긴다.**

> ⚠️ **파일명의 `-m1`을 믿지 말 것.** 규격은 기계와 무관하다 — `#15` 이후 그 접미사는 사실과 다르다.
> **이름은 바꾸지 않았다.** 바꾸면 기존 문서의 인용이 끊긴다. README에 한 줄로 적어 뒀다.
>
> ✅ **`font/` 추가됨** (2026-08-05 · 세아) — `a2-font-probe.ps1`(대조군 3종 포함) · `a3-latency-selfcheck.ps1`.
>
> ⛔ **제가 틀렸던 것**: 여기 *"세아의 재현 스크립트가 스크래치패드에 있다"* 고 적었는데,
> **애초에 파일이 없었다.** 세아는 `eqmux.exe`의 플래그를 셸에서 직접 쳐서 쟀고 스크립트로 남기지 않았다
> (스크래치패드 6곳에 `.ps1`이 한 개도 없음 — 세아 확인). **이관이 아니라 신설이다.**
> → *"어딘가에 있겠지"* 를 확인 없이 적었다. §3-3의 함정(*"기계가 같다고 가정"*)과 같은 종류다.
>
> 🔴 **대신 진짜로 휘발 위험인 것이 따로 있다** — **A-3 통과 판정의 원표본**(`lat500.jsonl`·`final.jsonl` ·
> 08-04 M1)이 **임시 폴더에 있다.** 관문 A가 이미 통과로 닫혔는데 그 근거의 원자료가 날아가면
> **재현 검증 자체가 불가능해진다** ([MACHINE-SPEC.md](MACHINE-SPEC.md) §6).
> → **해원 판단: 옮긴다.** `tools/gate-a/evidence/`에 **기계 코드를 붙여**(`a3-m1-…`) 넣는다.
> M2 원자료가 이미 그 폴더에 있어 층이 맞는다. 41 KB짜리라 저장소 부담도 없다. → 세아 회신함
>
> ⚠️ **`font/` 두 스크립트는 아직 한 번도 실행되지 않았다**(세아 기록). 제 10분 측정과 `eqmux.exe`를
> 공유하므로 **순서 합의가 필요하다.** A-4 격리 측정은 끝났고 잔여 0개라 그쪽으로는 안 막는다.

---

## 7. ✅ 이안에게 올렸던 것 넷 — **셋 닫힘 · 하나 진행 중**

| # | 무엇 | 결과 |
|---|---|---|
| 1 | **`GATE-A.md` §1 갱신** | ✅ **완료** (이안, 2026-08-05). 옛 본문은 §1-3에 기록으로 남았다 |
| 2 | **하니스 이관 여부** (§6) | ✅ **완료** — `tools/perf/` · 경계 규칙 4줄 |
| 3 | **`#7` 절차에 "사용량 기록" 추가** | ✅ **반영** — `#7` ② *"매 회 사용량 5항 동반"* |
| 4 | **EQMUX 10분 측정 창** | ⏳ **진행 중** — 관문은 안 막지만 세아의 `S2-1` 재빌드와 여전히 겹친다. **구간 합의 필요** |

---

## 8. ~~🔴 고치지 않고 올리는 것~~ → ✅ **셋 다 닫혔다** (2026-08-05 · [BRIEF-2026-08-05-KR3-M2.md](BRIEF-2026-08-05-KR3-M2.md) §0)

*"문서 정정 중 판정이 갈리는 자리가 나오면 고치지 말고 물어본다"*
([BRIEF-2026-08-05-SUPPORTER.md](BRIEF-2026-08-05-SUPPORTER.md) §5). **판정은 이안 몫이라 손대지 않았다.**
아래 둘은 **배정 브리프의 10곳에 없던 자리**다 — 정정하다 걸렸다.

### 8-1. ~~🔴~~ ✅ **KR3(P1 콜드스타트)에 A-4와 똑같은 기계 문제가 있다** — 결정됨: **M2 재측정**

> ### ✅ 결정 (이안, 2026-08-05) — **잰다. 다만 A-4에 이어서 재면 안 된다**
>
> `#15` 할 일 4(08-05 확장) · 상세: [BRIEF-2026-08-05-KR3-M2.md](BRIEF-2026-08-05-KR3-M2.md) §1·§2-1.
> 아래 본문 끝의 *"같은 격리 인스턴스에서 이어 돌리면 된다"* 는 **취소됐다** — 압력이 두 지표에
> **반대로 작용한다.** A-4는 워킹셋이 잘려 기준선이 낮아진다(엄해짐 = 보수적 ✅). KR3은 기동이
> 느려져 기준선이 높아진다(느슨해짐 = **유리** 🔴). A-4 종료 시점 여유 **1.89 GB**에서 이어 재면
> **판정을 유리하게 만드는 조건에서 기준선을 재는 것**이 된다 — §1-4 위반.
>
> **조건 셋** (브리프 §2-1): ① A-4와 **다른 창**에서 잰다 ② `Assert-MemoryHeadroom`(여유 ≥3 GB)을
> `coldstart-n10.ps1`에도 건다 ③ 시작·종료 압력을 A-4와 **같은 형식**으로 기록한다.
> 한계도 같이 적는다 — **M1의 476 ms는 어떤 압력에서 나왔는지 기록이 없다.**
> 측정 후 P1 현재값을 갱신한다.

`#15`는 A-4만 다뤘다. **KR3도 같은 구조다.**

| | A-4 (유휴 RAM) | **KR3 (콜드스타트)** |
|---|---|---|
| 판정 기준 | 기준선 **558.2 MB**(M1) × 40% | 기준선 **476 ms**(M1) **이하** (`#14`) |
| 판정 대상 | EQMUX — **M2**에서 잰다 | EQMUX — **M2**에서 잰다 |
| 처리 | ✅ `#15`가 **M2 재측정**으로 결정 | 🔴 **미결** |

`#14`는 측정 조건을 *"`BASELINE.md` §2.1 규격 그대로 — n=10 중앙값·p95 · **M1**"* 으로 못 박았다.
**그 조건대로 하면 M2에서 잰 EQMUX와 비교할 수 없고**, M2에서 다시 재면 `#14`의 조건을 벗어난다.

> **A-4를 재측정하기로 한 논리를 그대로 적용하면 KR3 기준선도 M2에서 다시 재야 한다.**
> 판정 시점이 **관문 A2와 같아서**(`S2-2` 이후) 시점도 겹친다 — 같은 격리 창에서 두 개를 한 번에 잴 수 있다.
> **결정해 주십시오.** 제가 정하면 그건 판정이 됩니다.
>
> ~~비용은 크지 않습니다 — A-4 재측정 때 `coldstart-n10.ps1`을 **같은 격리 인스턴스에서 이어 돌리면** 됩니다.~~
> ← 🔴 **이 제안이 틀렸다** (해원 자기정정). 압력 방향을 안 봤다 — A-4 종료 시점의 압력에서 이어 재면
> KR3 기준선이 유리한 쪽으로 부푼다. 브리프 §1이 취소했다. **위 결정 블록의 조건 셋이 정본이다.**

### 8-2. ✅ **재 봤다 — `#6`은 유효하다. 다만 안정화 30초가 M2에서는 부족하다** — 결정됨: **30초 유지** (`#17`)

> ### ✅ 결정 (이안, 2026-08-05) — **30초 유지. 개정 조건을 미리 못 박았다** (`#17` 신설)
>
> - 90초로 바꾸면 판정선이 219.8 → **219.3**으로 움직인다 — 폭 **0.5 MB**. 안정 구간 진동 폭(**6.4 MB**)의
>   1/12이다. **정밀해지는 게 아니라 정밀해 보이는 것**이다.
> - 바꾸면 M1 558.2·M3 552.5가 전부 **규격 불일치**가 된다 — 얻는 건 0.5 MB, 잃는 건 **대조점 3개**다.
> - 첫 표본 편향은 EQMUX 측정에도 **같은 방향**으로 걸리고, A-4는 40% **비율 판정**이라 나눗셈에서
>   상당 부분 지워진다. **EQMUX도 같은 30초 규격으로 잰다** — 규격의 목적은 두 앱을 **같은 자**로 재는 것이다.
> - 개정 조건 3건은 `#17`에 있다. 제일 중요한 건 1번 — *EQMUX 실측이 219.8 ± 6.4 MB 안에 들어와
>   진동 폭 안에서 합불이 갈리면(판정 불능) 다시 연다.*

> **[예고했던 것]** *"`#6`(안정화 30초 → 10표본 중앙값)은 M1(63.8 GB · 압력 낮음)에서 얻은 규격이다.
> M2는 15.3 GB다. 진동 폭이 M1 수준으로 수렴한다는 보장이 없다 — **재 보면 안다.**"*
> 결과가 나온 뒤에 규격을 바꾸면 판정이 아니기 때문에 **미리 적어 뒀던 자리**다.

**재 봤다. 진동 폭이 12.5 → 25.4 MB로 늘었다.** 예고한 대로 벌어졌지만 **원인이 분산이 아니다.**

| 구간 | 범위 | 폭 |
|---|---|---|
| 30s 포함 (현행 규격) | 545.4 ~ 570.8 | **25.4 MB** |
| **90s부터** | 545.4 ~ 551.8 | **6.4 MB** — M1(12.5)보다도 작다 |

**폭의 대부분을 첫 표본 하나가 만든다.** 약한 CPU·디스크라 초기 안정화가 더 걸린다.
**중앙값 규격이라 결과는 거의 안 흔들린다**(549.6은 90s 이후 구간 안에 있다).

> **그래서 `#6`을 고치지 않았다.** 결과를 보고 나서 규격을 손대면 그건 판정이 아니다(§1-4).
> 안건만 남긴다: **`#6`의 안정화 시간을 30초 → 90초로 올릴 것인가.** 지금 판정을 바꾸지 않으므로 급하지 않다.
> ~~→ 이안~~ → ✅ **`#17`로 확정 — 30초 유지** (위 결정 블록)

### 8-3. ~~🔴~~ ✅ **M3은 "접근 불가"가 아니다 — M2와 같은 기계로 보인다** — 결정됨: **동일 기계 인정** (`#16`)

> ### ✅ 결정 (이안, 2026-08-05) — **동일 기계 인정 · 552.5는 안 되살린다** (`#16`)
>
> ① **동일 기계 인정** — 근거는 아래 세 항목 실측. ② **552.5는 되살리지 않는다** — `#6` 10표본 규격
> **이전**(단일 표본 시절) 값이라 규격 불일치다. **대조군으로만** 쓴다. ③ 그 대조를 M2 재측정이 이행했다 —
> 549.6 vs 552.5 = **−0.5%**, 대조 성립 (`BASELINE.md` §3.6).
> *"대조가 맞았다"는 "되살려도 됐겠다"가 아니라 **"6분을 쓴 값이 옳았다"**는 뜻이다 — 값이 하나면
> 검산이 안 되고, 검산이 안 되는 값은 맞아도 맞은 줄 모른다* (브리프 §0-(3)).
> [MACHINE-SPEC.md](MACHINE-SPEC.md) §5·§6은 이 결정대로 재작성했다 — M3은 **"M2의 옛 이름"** 으로 남는다.

[MACHINE-SPEC.md](MACHINE-SPEC.md) §5는 *"지금 도는 세션 중 M2·M3에서 도는 것은 하나도 없다 →
M3 값은 재현 불가"* 로 닫혀 있다. **앞부분이 이미 `#15`로 뒤집혔고**(지금 여기가 M2다),
**뒷부분도 뒤집힐 것 같다.** 하니스 경로를 고치다 걸렸다.

**대조 — 세 항목이 전부 M3과 맞고 M1과 다르다.**

| 항목 | `BASELINE.md` §1·§6 (M3) | **지금 이 기계 (M2) 실측** | M1 실측 |
|---|---|---|---|
| OS | Windows 11 **Home 10.0.22631** | **Home 10.0.22631** ✅ | Pro 10.0.26200.8973 |
| AgentCommender 경로 | `Desktop\GitProject\ClaudeCodeTemplate\root\AgentCommender\ops` | **같은 경로** ✅ (라이브 프로세스에서 확인) | `D:\ClaudeCockpit\root\…` |
| `%APPDATA%\wmux` | **497.6 MB** | **497.61 MB** ✅ | 161.94 MB |

> **`wmux` 폴더 크기가 소수점 첫째 자리까지 같다.** 우연으로 보기 어렵다.
> OS 에디션·빌드가 같고, 앱 설치 경로까지 같다. **M2 = M3으로 판단한다.**

**사실이면 따라오는 것 셋** — 전부 판정에 닿는다.

| # | 무엇 |
|---|---|
| 1 | `MACHINE-SPEC.md` §5의 *"M3 재현 불가"* 와 §6의 갱신 규칙이 **다시 쓰여야 한다** |
| 2 | `BASELINE.md`의 M3 값(콜드스타트 n=2 · 유휴 RAM **552.5 MB** · 디스크)이 **재현 가능한 값**이 된다 |
| 3 | `#12`가 *"552.5는 M3 값이라 폐기"* 하고 M1에서 558.2로 재측정했는데, **판정 기계가 M2(=M3)로 온 지금은 552.5 쪽이 같은 기계 값**이다 |

> **그래도 `#15`의 M2 재측정은 그대로 해야 한다.** 552.5는 **단일 표본 시절 규격**이고
> `#6`(10표본 중앙값)으로 잰 값이 아니다. 대신 재측정 결과의 **대조군이 하나 생겼다** —
> 새 값이 552.5 근처면 규격 이행이 잘 된 것이고, 크게 벗어나면 **그 차이 자체를 설명해야 한다.**
>
> **제가 판단할 자리가 아닙니다.** 기계 동일성은 실측으로 확정했지만,
> *그래서 552.5를 되살릴 것인가* 는 판정입니다. ~~→ 이안~~ → ✅ **결정됨 — `#16` (위 결정 블록)**
