# EQMUX 구현 현황 — PRD 대조 (2026-08-12)

목업 전면 구현 후 실물화 1차(M1) 시점의 스냅숏.
기준: `docs/prd/00-index.md`의 11 PRD 맵. **실물** = 실제 데이터·프로세스로 동작 / **목업** = UI·상호작용 완성, 데이터는 `MockBackend` / **없음** = 화면·코드 모두 부재.

## 한눈에 보기

| PRD | 스코프 | 상태 | 비고 |
|---|---|---|---|
| **C** 세션 & 영속성 | 세션 소유·저장소·복구·종료 | 🟡 **부분 실물** | 핵심 축(스토어·스필·재생) 동작 |
| **A** 터미널 코어 | PTY·VT·렌더·스크롤백 | 🟡 **부분 실물** | ConPTY+xterm.js 실동작, VT 서버 모델 없음 |
| **B** 워크스페이스 셸 | 창·탭·페인 배치·레이아웃 영속 | 🟡 UI 실물 / 영속 없음 | 배치 6종·전체 화면 동작, 저장 안 됨 |
| **D** 에이전트 런타임 | Claude Code 실행·상태·재개·권한 | 🔴 **전부 목업** | 실물화 다음 최우선 후보 |
| **E** 팀·역할·임무 | 연결·편성·역할·임무 | 🟡 §4.1만 실물 | 연결 레지스트리 완성, 팀·임무 파일은 목 |
| **F** 메시지 버스 | 에이전트 간 메시지 | 🔴 목업 | 대화 탭 = 정적 메시지 |
| **G** 관제 & 대화 뷰 | 대시보드·알림·피드 | 🟡 UI 완성 / 데이터 목 | OS 알림·미확인 전파 등 미구현 |
| **H** 개발 도구 패널 | git·탐색기·포트·diff | 🟡 UI 완성 / 데이터 목 | 전 패널 정적 데이터 |
| **I** 외부 인터페이스 | CLI·MCP·훅 | 🔴 없음 | `eqmux` CLI 부재 |
| **J** 설정·데이터·보안 | 설정 스키마·시크릿 | 🔴 목업 | 설정 화면 표시만, 저장 없음 |
| **K** 배포·플랫폼 | 빌드·인스톨러·업데이트 | 🔴 없음 | bundle.active=false |

## 실물로 동작하는 것

### PRD C — 세션 & 영속성
| FR | 구현 |
|---|---|
| FR-C-01(부분) | PTY를 Rust가 단독 소유 — `pty_spawn/write/resize/kill`, UI는 invoke로만 접근 |
| FR-C-11·12 | VT 필터(`LineAssembler`) 통과 확정 줄만 스필 — 세션ID+단조 seq+ts |
| FR-C-20·20a | SQLite WAL · 7테이블 스키마 · 워크스페이스별 `<앱데이터>/workspaces/<id>/session.db` |
| FR-C-21 | 100ms 창 / 200행 배치 트랜잭션 (전용 쓰기 스레드) |
| FR-C-24 | `meta.schema_version` 기록 |
| FR-C-31·32 | 재시작 시 마지막 500줄 흐리게 재생 + 시각적 경계 |
| FR-C-50·51(부분) | 30일 · 세션당 10만 줄 상한, 백그라운드 정리 |
| FR-C-52(부분) | 저장 사용량 실측 (컨트롤 센터 스트립, 10초 갱신) |
| FR-C-03·07(부분) | 세션 메타(cwd·셸·생성·마지막 출력·누적 바이트) 기록 |
| (선행 1차) | 세션 원문 로그 파일 `~/.eqmux/logs/<세션>.log` append + 폴더 열기 |

**미구현 (C)**: FR-C-04(이름 규칙) · **05(Job Object 트리 정리 — 지금은 child.kill만)** · **06(웹뷰 크래시 생존/스냅샷)** · 09(메모리 계측) · 10(비활성 링버퍼 축소) · **13·14(위로 스크롤 시 디스크 페이징)** · 15(SGR 저장) · 16(FTS 검색) · 17(부분) · 22(레이아웃 JSON) · 23(assignment_cache) · 25(부분—event 테이블은 쌓이나 UI 미연결) · 26(command/OSC133) · **27(agent_session 재개 매핑)** · 28(손상 복구) · **30(레이아웃·포커스 복원)** · 33~35(재개 제안) · 40~45(named pipe·CLI 다중 접속) · **60~64(종료 flush 시퀀스)**

### PRD A — 터미널 코어 (부분)
- ConPTY 실셸 (pwsh → powershell → cmd 폴백, cwd 폴백), xterm.js 렌더 + EQMUX 테마
- 세션당 영속 터미널 인스턴스(리마운트 = DOM 재부착) — 스크롤백·상태 유지
- 리사이즈 디바운스 + 실변경시 PTY resize / 0-크기 마운트 가드
- **셸 선택 드롭다운** (PowerShell 7 · Windows PowerShell · Git Bash · cmd)
- **클립보드 네이티브(arboard)** — 텍스트 복사/붙여넣기, 이미지 → PNG 저장 후 경로 삽입
- **파일 드래그 앤 드롭** → 드롭한 페인에 경로 삽입
- 우클릭 메뉴 (복사·붙여넣기·모두 선택·화면 지우기)
- 미구현: VT 서버 모델(스냅샷), 셸 통합(OSC 133), 검색, 링크 감지

### PRD B — 워크스페이스 셸 (UI만 실물)
- 관제 고정 탭 + 워크스페이스 탭(닫기/추가) · 동시 오픈 10 제한(목 수준)
- 페인 배치 6종(그리드 행/열·스택·나열·메인+우측/하단) + Ctrl+Shift+L + 전체 화면(Nav 유지) + 줌
- 미구현: 배치·탭 상태 영속(FR-C-22·30), 분할선 드래그 비율 조정

### PRD E — 팀·역할·임무 (§4.1 실물)
| FR | 구현 |
|---|---|
| FR-E-01 | 폴더 선택 → git 검증 → 등록. 비저장소면 `git init` 확인 후 등록 |
| FR-E-02 | 원격 URL clone 후 자동 등록 |
| FR-E-03·04 | 목록(실측 브랜치·원격·마지막 사용) → 열기 → 탭 + 흐름 진입 |
| FR-E-07 | `<앱데이터>/workspaces.json` 원자적 쓰기 |
| FR-E-08 | 경로 소실 감지 → 재지정(폴더 재선택) |
| FR-E-09 | 등록 해제 = 레지스트리만 삭제 |

**미구현 (E)**: FR-E-05(오픈 10 하드 제한 실측) · **10~17(team.json/team.md 실파일 읽기·쓰기)** · 20~
(역할 라이브러리 실파일·전역/워크스페이스 2단) · 30~(임무 `.eqmux/missions/*.md` 실파일 + watch) · 40~(역할 합성·주입) — 캐스팅/편성/역할/임무 화면은 전부 목 데이터로 동작

### 슬롯 CRUD (E 편성의 앱 내 표현 — 목 상태에서 동작)
- 빈 슬롯 → 기본 터미널 / 역할 세션(사용 중 페르소나 제외) 2택 생성
- 역할 부여/변경/해제 (직무 변경 → 재시작 필요 E11′ 흐름)
- ✕ 제거: 기본 터미널 즉시 / 역할 세션 확인 다이얼로그
- ※ team.json에 아직 안 씀 — 세션 목록은 메모리

## 목업으로만 있는 것 (UI 완성, 데이터 목)

| 영역 | 화면 | 실물화에 필요한 것 |
|---|---|---|
| **D 전부** | 상태 6종·waiting/재개/권한 플래그 표시 | Claude Code 기동(FR-D-01~09), 레지스트리 watch(10~19), 재개(20~27), 훅(30~38), 권한 번역 적용(40~45) |
| G 관제 | 대시보드·요약 타일·주의 순 정렬·이벤트 피드 | D 상태 스트림 + C event 테이블 연결, OS 알림(30~37), 미확인 전파(44~47) |
| F 대화 | 대화 탭·강제 5종 메시지 | 메시지 버스 자체 |
| H git 패널 | 브랜치·커밋 그래프·diff 요약 | git CLI 연동 (레지스트리의 git 헬퍼 재사용 가능) |
| H diff 에디터 | 3분할 비교·스테이지 | git diff 파싱 + 에디터 |
| H 포트 | 세션/시스템 포트 | netstat/iphlpapi 폴링 |
| H 로그 패널 | 서버 로그 스트림 | store event 테이블 실연결 (내보내기·세션 로그 폴더는 실물) |
| H 탐색기(임무) | 파일 트리·미리보기 | 실제 FS 읽기 + .eqmux watch |
| H 브라우저 | 자리만 | 웹뷰 임베드 |
| 트랜스크립트 | 턴 뷰·입력 | Claude 트랜스크립트 파일 파싱(V2 — 참조만) |
| J 설정 | 항목 표시·값 토글 | 설정 스키마 + 저장 |
| 종료 확인 | 다이얼로그 | 실행 세션 열거 + flush 시퀀스(FR-C-60~64) |

## 다음 실물화 추천 순서

1. **PRD D 에이전트 런타임** — 이 앱의 존재 이유. `claude --session-id` 기동 → 세션 레지스트리 watch → 상태 6종 실측 → 재개. C의 agent_session 테이블(27)과 종료 다이얼로그가 여기 걸려 있음
2. **E 팀 파일 실물화** — team.json/team.md 읽기·쓰기 (슬롯 CRUD가 이미 UI를 갖춤), .eqmux/missions 실파일
3. **G 관제 실연결** — D 상태 스트림 + C event 테이블 → 대시보드·피드·OS 알림
4. **H git 패널 실데이터** — 레지스트리 git 헬퍼 확장으로 저비용
5. C 잔여 (Job Object, 디스크 페이징, 재개 매핑, 종료 flush)
