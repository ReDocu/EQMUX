# 명령어 정리

EQMUX에는 CI가 없다. 커밋도 빌드도 릴리스도 손으로 한다. 이 문서는 그 손 순서다.

셸은 두 가지를 섞어 쓴다 — Git Bash(POSIX)와 PowerShell. 여러 줄 문자열 문법이 다르므로
각 절에 어느 셸에서 치는 명령인지 적어 두었다.

---

## 0. 한눈에

| 하고 싶은 것 | 명령 |
|---|---|
| 개발 서버(웹 목업) | `npm run dev` |
| 실제 앱 띄우기 | `npm run tauri dev` |
| 타입 검사 | `npx tsc --noEmit` |
| 프런트 테스트 | `npm test` |
| Rust 테스트 | `cd src-tauri && cargo test --lib` |
| 프런트 프로덕션 빌드 | `npm run build` |
| 릴리스 바이너리 | `npm run tauri build -- --no-bundle` |
| 릴리스 목록 | `gh release list --limit 5` |

---

## 1. 개발

```bash
npm install                 # 최초 1회 · package-lock.json 기준
npm run dev                 # Vite만 — 브라우저에서 목 데이터로 UI 확인 (PTY·FS 없음)
npm run tauri dev           # 실제 앱 — Rust까지 빌드해서 창을 띄운다 (첫 실행은 몇 분)
```

`npm run dev`는 `isTauri()`가 false인 목업 경로다. PTY·파일 시스템·스토어가 필요한 변경은
반드시 `npm run tauri dev`로 확인한다.

---

## 2. 커밋 전 검증

네 가지를 통과시키고 커밋한다. 앞의 둘은 빠르고, Rust 테스트는 첫 컴파일만 오래 걸린다.

```bash
npx tsc --noEmit                      # 타입 (출력이 없으면 통과)
npm test                              # vitest — node 환경, 백엔드 순수 로직
cd src-tauri && cargo test --lib      # Rust — store·workspace·diff 등
cd .. && npm run build                # tsc + vite build (번들까지 실제로 만들어 본다)
```

특정 Rust 모듈만 빠르게 보고 싶으면 이름으로 거른다.

```bash
cd src-tauri && cargo test --lib store::      # store 모듈 테스트만
```

---

## 3. 커밋

### 메시지 규약

- **영어**, 명령형, 문장형 대소문자. `feat:` 같은 conventional-commit 접두사는 쓰지 않는다.
- 제목은 *무엇을 했는지*, 본문은 *왜 그렇게 했는지*를 산문으로. 목록 나열이 아니라 문단.
- 트레일러 두 줄:

```
Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_<id>
```

- 기능 커밋과 버전 올리기 커밋은 **분리한다**. 버전 커밋 제목은 `Bump the version to X.Y.Z`.

### 명령 (Git Bash)

```bash
git status --short                    # 무엇이 올라가는지 먼저 본다
git add -A
git commit -F - <<'EOF'
Cut stored lines to the newest thousand

여기에 왜 그렇게 했는지 문단으로.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_<id>
EOF
git log --oneline -3
git push origin main                  # 이 저장소는 main에 직접 푸시한다 (PR 없음)
```

### 명령 (PowerShell)

PowerShell에는 heredoc이 없다. 작은따옴표 here-string을 쓰고, 닫는 `'@`는 **줄 맨 앞**에 둔다.

```powershell
git commit -m @'
Cut stored lines to the newest thousand

본문.
'@
```

---

## 4. 릴리스 배포

배포물은 **포터블 zip 하나**다. `tauri.conf.json`의 번들 타깃은 `nsis`지만 설치본은 배포하지 않는다.
`npm run tauri build`를 그냥 치면 NSIS 설치본까지 만드느라 오래 걸리므로 `--no-bundle`을 붙인다.

### 4-1. 버전 올리기 — 파일 네 개

| 파일 | 고치는 곳 |
|---|---|
| `package.json` | `"version": "X.Y.Z"` |
| `src-tauri/tauri.conf.json` | `"version": "X.Y.Z"` |
| `src-tauri/Cargo.toml` | `version = "X.Y.Z"` |
| `src-tauri/Cargo.lock` | **직접 고치지 않는다** — cargo가 갱신한다 |

```bash
# 앞의 세 파일을 고친 뒤, lock은 cargo 명령 한 번으로 따라오게 한다
cd src-tauri && cargo metadata --format-version 1 --offline > /dev/null && cd ..
git status --short                    # 정확히 네 파일만 바뀌어야 한다
git add package.json src-tauri/Cargo.lock src-tauri/Cargo.toml src-tauri/tauri.conf.json
git commit -m "Bump the version to X.Y.Z"
git push origin main
```

설정 화면의 `정보 > 버전`은 `package.json`을 빌드 타임에 읽는다(`vite.config.ts`의 `__APP_VERSION__`).
따로 고칠 곳은 없다.

### 4-2. 빌드

```bash
npm run tauri build -- --no-bundle
# 결과: src-tauri/target/release/eqmux.exe
```

- **실행 중인 EQMUX가 그 exe라면 링크 단계에서 파일 잠금으로 실패한다.** 앱을 닫고 빌드한다.
- 첫 릴리스 빌드는 10분 안팎, 이후 증분은 훨씬 짧다.

### 4-3. 포터블 zip 만들기 — 항목은 두 개뿐

zip 안의 구조는 항상 이 둘이다.

```
EQMUX/eqmux.exe
EQMUX/README.txt
```

`README.txt`는 저장소에 없다. **직전 릴리스 자산에서 꺼내 이어 쓴다** — 맨 윗줄의 버전을 고치고,
맨 아래에 이번 버전 항목을 한 덩어리 덧붙인다(사용자가 체감하는 변화만, 한국어 서술형).

```bash
# 직전 릴리스에서 README.txt 가져오기
gh release download v<이전버전> --repo ReDocu/EQMUX --clobber
unzip -o EQMUX_<이전버전>_x64_portable.zip -d prev
cp prev/EQMUX/README.txt ./README.txt      # 열어서 버전 줄 수정 + 새 항목 추가
```

```powershell
# 스테이징 폴더를 만들어 두 파일만 넣고 압축 (PowerShell)
New-Item -ItemType Directory -Force stage\EQMUX
Copy-Item src-tauri\target\release\eqmux.exe stage\EQMUX\
Copy-Item README.txt stage\EQMUX\
Compress-Archive -Path stage\EQMUX -DestinationPath EQMUX_X.Y.Z_x64_portable.zip -Force
```

압축 뒤 항목이 둘인지 확인한다.

```bash
unzip -l EQMUX_X.Y.Z_x64_portable.zip
```

### 4-4. 릴리스 만들기

노트는 **한국어 서술형**으로 쓴다 — 무엇이 바뀌었고 그래서 무엇이 달라지는지.

```bash
gh release create vX.Y.Z \
  --repo ReDocu/EQMUX \
  --target <버전 커밋의 전체 sha> \
  --title "EQMUX X.Y.Z" \
  --notes-file notes.md \
  EQMUX_X.Y.Z_x64_portable.zip
```

`--target`은 그 버전을 담은 커밋(보통 `Bump the version to X.Y.Z`)의 sha다. **짧은 sha는 거부된다** —
`HTTP 422: Release.target_commitish is invalid`가 뜬다. 전체 sha를 쓰고, 그 커밋이 이미
푸시돼 있어야 한다.

```bash
git rev-parse HEAD                    # 전체 sha
git ls-remote origin main             # 원격에 올라가 있는지 확인
```

### 4-5. 확인

```bash
gh release list --limit 3
gh release view vX.Y.Z
```

---

### 4-6. 실제로 밟은 순서 (0.3.7)

이 문서는 0.3.7을 내면서 그대로 밟아 본 것이다. 요약하면 다음 열 줄이다.

```bash
# 1) 기능 커밋 → 2) 버전 커밋 → 3) 푸시
git add -A && git commit -F - <<'EOF' ... EOF
# package.json · tauri.conf.json · Cargo.toml 수정
cd src-tauri && cargo metadata --format-version 1 --offline > /dev/null && cd ..
git add package.json src-tauri/Cargo.lock src-tauri/Cargo.toml src-tauri/tauri.conf.json
git commit -m "Bump the version to 0.3.7" && git push origin main

# 4) 빌드 (약 1분 30초, 증분)
npm run tauri build -- --no-bundle

# 5) 직전 릴리스에서 README.txt 이어받기 → 6) zip → 7) 릴리스
gh release download v0.3.6 --repo ReDocu/EQMUX --clobber
unzip -o EQMUX_0.3.6_x64_portable.zip -d prev      # prev/EQMUX/README.txt 를 고쳐 쓴다
# (PowerShell) Compress-Archive -Path stage\EQMUX -DestinationPath EQMUX_0.3.7_x64_portable.zip -Force
unzip -l EQMUX_0.3.7_x64_portable.zip              # 항목이 정확히 2개인지
gh release create v0.3.7 --repo ReDocu/EQMUX --target <full sha> \
  --title "EQMUX 0.3.7" --notes-file notes.md EQMUX_0.3.7_x64_portable.zip
```

## 5. 자주 쓰는 확인

```bash
git log --oneline -5                        # 최근 커밋
git show --stat HEAD                        # 마지막 커밋이 만진 파일
git diff --stat                             # 아직 커밋 안 한 변경 규모
gh release list --limit 5                   # 릴리스 이력
```
