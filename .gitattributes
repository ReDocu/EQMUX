# 라이선스 전문은 줄바꿈 변환을 하지 않는다.
# 이 기계는 core.autocrlf=true라 그냥 두면 체크아웃 때 LF→CRLF로 바뀌고,
# NOTICE-D2Coding.md에 적어 둔 sha256(정본 대조용)이 재현되지 않는다.
# 법적 문서라 "내용은 같다"로 넘기지 않고 바이트를 고정한다.
public/licenses/LICENSE-OFL-1.1.txt -text

# design.pen은 암호화 산출물이다 — 텍스트로 취급하면 안 된다.
# core.autocrlf=true라 속성 없이 두면 체크아웃 때 LF→CRLF가 파일 내부에 주입되고,
# Pencil이 쓴 바이트와 달라진다(새로 클론한 트리에서만 터진다).
design.pen -text -diff
